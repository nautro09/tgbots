import os
import telebot
import requests
from telebot import types
from datetime import datetime, timedelta
from bs4 import BeautifulSoup
import random
import time
import re
import requests
import json
from telebot.types import InlineKeyboardMarkup, InlineKeyboardButton
import html  # библиотека для экранирования символов
import threading
from PIL import Image
import io
import sqlite3
import math
import logging
from youtube_search import YoutubeSearch
import pyshorteners
from googletrans import Translator
from telebot.types import Message


# API ключ OpenWeatherMap
API = os.getenv('API', 'YOUR_API_KEY')

TOKEN = os.getenv('TOKEN','YOUR_TOKEN') 
bot = telebot.TeleBot(TOKEN)


# Список запрещенных слов
BAD_WORDS = ['плохое1', 'плохое2', 'плохое3','член']

# Счетчик слов
message_counter = 0
user_activity = {}

# Словарь для отслеживания состояния спама
spam_status = {}
# Словарь для отслеживания забаненных пользователей и их срока мута
muted_users = {}

# Хранилище для отложенных уведомлений (словарь)
scheduled_notifications = {}

# Временные интервалы
timeslots = ['7:00', '9:00', '13:00', '16:00', '20:00']

# Дни недели
days_of_week = ['Понедельник', 'Вторник', 'Среда', 'Четверг', 'Пятница', 'Суббота', 'Воскресенье']

# Переменная для хранения текущего состояния настроек
user_scheduling = {}

#Активные сессии для математики
active_sessions = set()


# Словарь для хранения информации о пользователях
translator = Translator()
shortener = pyshorteners.Shortener()

# Emojis
EMOJI_CLOCK = '⏲'
EMOJI_CALENDAR = '📆'
EMOJI_ARROW = '➡'
EMOJI_CHECK = '✅'
EMOJI_CROSS = '❌'
EMOJI_EDIT = '✏'
EMOJI_DELETE = '🗑️'
EMOJI_INFO = 'ℹ'
heart_emoji = "❤"
dice_emoji = "🎲"
ghost_emoji = "👻"
mask_emoji = "🎭"
robot_emoji = "🤖"
gift_emoji = "🎁"

# Список взаимодействий с описанием для удачи и неудачи
interactions = {
    "убить": {"success": "💀 @{target} был убит @{sender}!", "fail": "🤕 @{sender} пытался убить @{target}, но потерпел неудачу!"},
    "поцеловать": {"success": "😘 @{sender} поцеловал @{target}! Как мило!", "fail": "💔 @{target} отверг поцелуй @{sender}."},
    "обнять": {"success": "🤗 @{sender} обнял @{target}. Тепло и уютно!", "fail": "🙅 @{target} не захотел обнимать @{sender}."},
    "ударить": {"success": "👊 @{sender} ударил @{target}! Больно!", "fail": "😬 @{sender} промахнулся и ударил себя."},
    "заразить": {"success": "😷 @{sender} заразил @{target} вирусом! Берегись!", "fail": "🦠 @{target} оказался невосприимчивым!"},
    "сжечь": {"success": "🔥 @{sender} сжёг @{target}! Пепел остался!", "fail": "💦 @{target} успел потушить огонь!"},
    "застрелить": {"success": "🔫 @{sender} застрелил @{target}! Взрыв эмоций!", "fail": "😲 @{target} увернулся от выстрела!"},
    "рассмешить": {"success": "😂 @{sender} рассмешил @{target}. Какой юморист!", "fail": "😐 @{target} остался равнодушным к шуткам @{sender}."},
    "кастрировать": {"success": "✂️ @{sender} кастрировал @{target}! 😱", "fail": "🙅 @{target} оказался слишком ловким и избежал кастрации."},
    "взбесить": {"success": "😡 @{sender} сильно бесит @{target}!", "fail": "😏 @{target} не поддался раздражению."},
    "испугать": {"success": "👻 @{sender} испугал @{target} до дрожи!", "fail": "😎 @{target} не испугался выходки @{sender}."},
    "похвалить": {"success": "👏 @{sender} похвалил @{target} за хорошую работу!", "fail": "😕 @{target} не оценил похвалу от @{sender}."},
    "пнуть": {"success": "🥾 @{sender} пнул @{target} с силой!", "fail": "😂 @{target} увернулся от удара @{sender}."},
    "подарить": {"success": "🎁 @{sender} подарил @{target} замечательный подарок!", "fail": "😞 @{target} не принял подарок от @{sender}."}
}
# Список характеристик с описаниями
characteristics = [
    ("Смелый", "всегда готов к приключениям", "🦁"), ("Дружелюбный", "всегда рад новым знакомствам", "🤝"),("Загадочный", "никто не знает его секретов", "🕵️"),
    ("Весёлый", "с ним всегда праздник", "😂"),("Трудолюбивый", "готов работать 24/7", "🛠️"),("Креативный", "найдет выход из любой ситуации", "🎨"),
    ("Интеллектуальный", "всегда имеет умный план", "🧠"),("Энергичный", "его хватит на всех", "⚡"),("Мудрый", "словно древний философ", "🧙‍♂️"),
    ("Непредсказуемый", "никогда не знаешь, что будет дальше", "🎲"),("Оптимистичный", "смотрит на жизнь через розовые очки", "☀️"),("Честный", "всегда говорит правду", "🏅"),
    ("Скромный", "любит быть в тени", "😇"),("Злопамятный", "никогда не забудет обиду", "😡"),("Паникёр", "его легко испугать", "🚨"),("Ленивый", "спит и ест", "🛋️"),
    ("Нервный", "всегда на взводе", "😬"),("Туповатый", "но зато добрый", "🐑"),("Завистливый", "следит за успехами других", "👀"),("Эгоистичный", "всегда думает о себе", "🙃"),
    ("Меланхоличный", "грустит по вечерам", "🌙"),("Философский", "глубокие мысли в голове", "🤔"),("Хвастливый", "любит показывать свои достижения", "💪"),
    ("Слишком подозрительный", "считает, что за ним следят", "👀"),("Романтичный", "влюбляется в каждый закат", "🌅"),("Циничный", "во всём видит подвох", "🧩"),
    ("Саркастичный", "его ирония режет как нож", "🗡️"),("Добрый", "не пройдёт мимо чужой беды", "🧸"),("Надёжный", "на него можно положиться", "🛡️"),("Сонный", "ждёт шанса вздремнуть", "😴"),
    ("Прямолинейный", "в лоб и по факту", "🪓"),("Безбашенный", "сначала делает — потом думает", "🎢"),("Мечтательный", "живет на облаке фантазий", "☁️"),
    ("Реалист", "всегда смотрит правде в глаза", "🔍"),("Педантичный", "заметит каждую пылинку", "🧼"),("Гиперактивный", "ему и кофе не нужен", "🦿"),("Спонтанный", "решения приходят внезапно", "💡"),
    ("Холодный", "как айсберг в океане эмоций", "🧊"),("Тёплый", "в его объятиях уютно", "🔥"),("Целеустремлённый", "всегда идёт к своей цели", "🎯"),
    ("Амбициозный", "мечтает о мировом господстве", "🌍"),("Чудаковатый", "немного странный, но обаятельный", "🛸"),("Игривый", "шалит по настроению", "😼"),
    ("Злополучный", "приключения сами его находят", "🪤"),("Заботливый", "всегда помнит, кто что любит", "🍵"),
    ("Мстительный", "планирует отомстить в 2037 году", "📅"),("Рассудительный", "всё обдумает трижды", "📘"),("Авантюрный", "жизнь — его квест", "🎮"),
    ("Скептичный", "ничему не верит с первого раза", "🧐"),("Залипательный", "на него можно смотреть бесконечно", "🌀"),("Творческий", "у него в голове фейерверк", "🎆"),
    ("Сладкоежка", "жизнь — это десерт", "🍰"),("Любопытный", "всё хочет знать и проверить", "🔎"),("Молчаливый", "говорит только, когда нужно", "🤫"),
    ("Драматичный", "даже чай пьёт с надрывом", "🎭"),("Самоироничный", "шутит над собой лучше всех", "🪞"),("Ловкий", "вечно выкручивается", "🤹"),
    ("Хитрый", "придумает, как выйти сухим из воды", "🦊"),("Забывчивый", "всё помнит, кроме важного", "🧠❌"),("Модный", "как с обложки глянца", "🕶️"),("Расслабленный", "вечно на чиле", "🛀"),
    ("Бесстрашный", "ничего не боится... почти", "🦇"),("Стратегичный", "в голове — шахматная доска", "♟️"),("Остроумный", "всегда найдёт, что ответить", "💬"),
    ("Терпеливый", "ждёт, когда все успокоятся", "⏳"),("Забавный", "с ним не соскучишься", "🤣"),("Неукротимый", "как ветер в поле", "🌪️"),("Скромный", "не любит хвастаться", "🙈"),
    ("Тайный", "всё скрывает за маской", "🎭"),("Эмоциональный", "всегда на волне чувств", "🌊"),("Спокойный", "как божья коровка на лужайке", "🐞"),("Болтливый", "разговор поддержит даже с лампой", "🗣️"),
    ("Тусовочный", "там, где он — там и движ", "🎉"),
    ("Романтичный циник", "цветы подарит, а потом пошутит", "🌹😏"),("Чистюля", "всё должно быть по линеечке", "🧽"),("Раздражительный", "его бесит даже Wi-Fi", "📶😠"),
    ("Фантазёр", "придумал бы «Гарри Поттера», если бы успел", "📖✨"),("Молчаливый стратег", "всё видит, но молчит", "🧊♟️"),("Вечный студент", "вечно что-то учит", "📚"),
    ("Уверенный", "заходит в комнату — и все это чувствуют", "🕴️"),
    ("Сомневающийся", "а вдруг нет?..", "🤷"),("Непреклонный", "его не сдвинуть с места", "🪨"),("Беспокойный", "даже в покое тревожен", "🌀"),
    ("Везунчик", "ему просто фартит", "🍀"),
    ("Пофигист", "ничто не тревожит его дзен", "🧘"),("Обидчивый", "помнит, кто не поставил лайк", "📱💔"),
    ("Обаятельный", "улыбнётся — и сердце тает", "😊"),("Медлительный", "всё делает в своём темпе", "🐢"),
    ("Одержимый идеей", "не остановится, пока не добьётся", "🎯🔥"),("Интуитивный", "ощущает, что пицца скоро приедет", "🔮🍕"),
    ("Занудный", "знает все правила и напомнит тебе", "📏"),("Сам себе праздник", "с ним весело, даже в понедельник", "🎊"),
    ("Параноик", "и микрофон под подушкой проверил", "🎙️😳"),("Фанатичный", "всё или ничего", "💣"),
    ("Приятный во всём", "даже кактусы его обожают", "🌵❤️"),("Эмпатичный", "чувствует чужое настроение сильнее своего", "🤝💞"),
    ("Прагматик", "считает даже мечты в процентах", "📈"),
    ("Апатичный", "может не встать даже ради торта", "🛌"),
    ("Обходительный", "умеет найти общий язык с кем угодно", "🤗"),("Идеалист", "хочет, чтобы всё было как в кино", "🎬"),("Экспрессивный", "говорит не только словами, но и руками", "👐"),
    ("Контролирующий", "всё под его контролем (или почти)", "🎛️"),("Бунтарь", "делает наоборот просто потому что может", "🚫🔥"),
    ("Праздный", "любит отдыхать больше, чем работать", "🍹"),("Честолюбивый", "мечтает о пьедестале", "🥇"),("Переживательный", "волнуется даже за погоду на Марсе", "🪐😟"),
    ("Быстро загорается", "вчера — идея, сегодня — проект", "⚡🧠"),("Бывает в облаках", "но с очень красивым видом", "☁️👀"),
    ("Неунывающий", "его не сломает даже понедельник", "💪🙂"),("Молчаливый философ", "молчит — значит, думает о вечном", "🌌🤫"),
    ("Беспредельно добрый", "даже муху накормит", "🐞🍞"),("Смелый", "прыгает в неизвестность с улыбкой", "🛡️"),
    ("Дружелюбный", "знает, как найти общий язык даже с кактусом", "🌵🤗"),("Загадочный", "появляется и исчезает, оставляя вопросы", "🌫️"),
    ("Весёлый", "вносит радость в любое помещение", "🎈"),("Трудолюбивый", "работает так, будто ему это в кайф", "💼🔥"),
    ("Креативный", "видит мир в формах и цветах, которых никто не замечает", "🧶"), ("Интеллектуальный", "рассуждает так, что голова идёт кругом — в хорошем смысле", "📚🧩"),("Энергичный", "заряжает всех, как ходячий аккумулятор", "🔋"),
    ("Мудрый", "говорит мало, но метко", "🐢"),("Непредсказуемый", "его план — отсутствие плана", "🎯❓"),("Оптимистичный", "видит солнце даже в пасмурную погоду", "🌈"),
    ("Честный", "всегда говорит как есть, даже если неловко", "🔎"),
    ("Скромный", "делает великое в тени", "🌑"),
    ("Злопамятный", "внутри — целый архив обид", "🗃️"),
    ("Паникёр", "сначала кричит, потом думает", "🙀"),
    ("Ленивый", "в движении — экономит энергию по максимуму", "🦥"),
    ("Нервный", "дергается, даже когда всё по плану", "🐿️"),
    ("Туповатый", "простой, но искренний", "🥔"),
    ("Завистливый", "вечно сравнивает, и не в свою пользу", "📊"),
    ("Эгоистичный", "мир крутится вокруг него — по его же версии", "🪞"),
    ("Меланхоличный", "ходит с дождём внутри", "🌧️"),
    ("Философский", "всегда ищет смысл, даже в котлетах", "🥩🤔"),
    ("Хвастливый", "любит похвалить себя, особенно громко", "📢"),
    ("Слишком подозрительный", "всех проверит трижды", "🔍🕵️‍♂️"),
    ("Романтичный", "влюблён в любовь", "💌"),
    ("Циничный", "смотрит сквозь призму сарказма", "🧂"),
    ("Саркастичный", "его ирония — тоньше лезвия", "🔪"),
    ("Добрый", "сердце размером с планету", "🌍❤️"),
    ("Надёжный", "как якорь среди шторма", "⚓"),
    ("Сонный", "всегда в режиме ожидания", "💤"),
    ("Прямолинейный", "как удар в лоб — честно, но больно", "📏"),
    ("Безбашенный", "делает, а потом думает", "🚀"),
    ("Мечтательный", "живёт в мире, где всё возможно", "🎠"),
    ("Реалист", "видит вещи как есть, не как хочется", "🪞"),
    ("Педантичный", "знает, где что должно лежать", "🧾"),
    ("Гиперактивный", "везде одновременно — и хочет больше", "🎮"),
    ("Спонтанный", "решает на ходу — и это работает", "🛣️"),
    ("Холодный", "держит дистанцию и не паникует", "❄️"),
    ("Тёплый", "в его компании всегда уютно", "☕"),
    ("Целеустремлённый", "видит цель даже сквозь туман", "🗻"),
    ("Амбициозный", "мечтает о многом и берёт своё", "🚁"),
    ("Чудаковатый", "не от мира сего — и в этом кайф", "🧦"),
    ("Игривый", "шутит так, что не знаешь, серьёзно ли", "🙃"),
    ("Злополучный", "проблемы сами его находят", "🧲😅"),
    ("Заботливый", "спросит, поел ли ты — 10 раз", "🍲"),
    ("Мстительный", "никогда не забывает и не прощает", "🧾😈"),
    ("Рассудительный", "спокойно разложит всё по полочкам", "🧠🗂️"),
    ("Авантюрный", "жизнь для него — вечный квест", "🎒"),
    ("Скептичный", "не верит, пока сам не проверит", "📉"),
    ("Залипательный", "его слушают, затаив дыхание", "🧲🗨️"),
    ("Творческий", "превращает обыденное в искусство", "✏️"),
    ("Сладкоежка", "голосует за десерт в любом споре", "🍫"),
    ("Любопытный", "вечно лезет туда, где интересно", "🪤"),
    ("Молчаливый", "говорит мало — но по делу", "📵"),
    ("Драматичный", "переживает на уровне театра", "🧵🎭"),
    ("Самоироничный", "смеётся над собой громче всех", "🪞😂"),
    ("Ловкий", "выпутается из любой ситуации", "🐍"),
    ("Хитрый", "всегда найдёт обходной путь", "🎯↩️"),
    ("Забывчивый", "держит всё в голове, но не всегда найдёт", "🧠🫠"),
    ("Модный", "знает тренды раньше, чем они стали модой", "👟"),
    ("Расслабленный", "не парится вообще ни о чём", "🌴"),
    ("Бесстрашный", "не знает, что такое «опасно»", "🦍"),
    ("Стратегичный", "просчитывает всё заранее", "📋"),
    ("Остроумный", "его фразы — как искры", "💥"),
    ("Терпеливый", "выжидает, пока мир сам сдастся", "🧘‍♂️"),
    ("Забавный", "его приколы становятся легендами", "🗿"),
    ("Неукротимый", "идёт напролом — и улыбается", "🚜"),
    ("Скромный", "спрячется за занавеской, даже если герой", "🎭"),
    ("Тайный", "никто не знает, что у него на уме", "🧳"),
    ("Эмоциональный", "чувствует всё сразу — и сильно", "🌋"),
    ("Спокойный", "его не тревожит даже апокалипсис", "🌌"),
    ("Болтливый", "разговор — его стихия", "📣"),
    ("Тусовочный", "всегда в центре событий", "🕺"),
    ("Романтичный циник", "любит — но с сарказмом", "🖤🌹"),
    ("Чистюля", "каждую пылинку назовёт по имени", "🧼"),
    ("Раздражительный", "его бесит даже тишина", "🔇😠"),
    ("Фантазёр", "расскажет историю, в которую сам почти верит", "🧚‍♂️"),
    ("Молчаливый стратег", "тихо движется — но точно", "🎯🤫"),
    ("Вечный студент", "учится, даже когда уже знает", "📖🔁"),
    ("Уверенный", "шагает так, будто знает результат", "🧍‍♂️✨"),
    ("Сомневающийся", "всё проверяет на три раза", "🔄"),
    ("Непреклонный", "гнётся — но не ломается", "🪵"),
    ("Беспокойный", "думает о всём сразу", "🎧🧠"),
    ("Везунчик", "падает в грязь — и находит монету", "💫"),
    ("Пофигист", "жизнь идёт — и ладно", "🌊"),
    ("Обидчивый", "помнит обиды по секундам", "📅😿"),
    ("Обаятельный", "обворожителен без усилий", "😌"),
    ("Медлительный", "движется в своём ритме", "⏳"),
    ("Одержимый идеей", "горит, пока не сгорит", "🔥📌"),
    ("Интуитивный", "чувствует ответы без объяснений", "👁️‍🗨️"),
    ("Занудный", "инструкция важнее веселья", "📑"),
    ("Сам себе праздник", "ходит с конфетти в карманах", "🎇"),
    ("Параноик", "даже зеркало вызывает подозрения", "📹"),
    ("Фанатичный", "если верит — то до конца", "📣🔥"),
    ("Приятный во всём", "как плед в холодный вечер", "🧣"),
    ("Эмпатичный", "чувствует твоё настроение раньше тебя", "🫶"),
    ("Прагматик", "видит выгоду даже в чае", "💼☕"),
    ("Апатичный", "не волнует даже то, что должно", "🌫️"),
    ("Обходительный", "улыбнётся даже в споре", "🤝😄"),
    ("Идеалист", "ищет совершенство даже в деталях", "📐"),
    ("Экспрессивный", "говорит — и сразу с эмоцией", "🎙️💥"),
    ("Контролирующий", "держит всё под пальцем", "📊🕹️"),
    ("Бунтарь", "против всего, особенно против скуки", "🧨"),
    ("Праздный", "отдыхает, даже когда работает", "🏖️"),
    ("Честолюбивый", "мечтает быть первым — всегда", "🥇"),
    ("Переживательный", "волнуется за всех и всё", "💭"),
    ("Быстро загорается", "идея — и уже в действии", "🚴💡"),
    ("Бывает в облаках", "улетает мыслями внезапно", "🌤️"),
    ("Неунывающий", "всегда найдёт повод для улыбки", "😄✌️"),
    ("Молчаливый философ", "молчит так, что всё понятно", "📜"),
    ("Беспредельно добрый", "обнимет даже воробья", "🕊️💛"),
    ("Смелый", "не боится быть первым", "🦁"),
    ("Дружелюбный", "всегда протянет руку помощи", "🤝"),
    ("Загадочный", "всегда с тайной улыбкой", "🕵️"),
    ("Весёлый", "его смех заразителен", "😂"),
    ("Трудолюбивый", "работает, пока другие отдыхают", "🛠️"),
    ("Креативный", "думает вне коробки", "🎨"),
    ("Интеллектуальный", "всегда найдёт логичный ответ", "🧠"),
    ("Энергичный", "не умеет сидеть на месте", "⚡"),
    ("Мудрый", "слова его — как пословицы", "🧙‍♂️"),
    ("Непредсказуемый", "его поступки — как фейерверк", "🎲"),
    ("Оптимистичный", "даже в лужах видит отражение солнца", "☀️"),
    ("Честный", "не умеет врать, даже во вред себе", "🏅"),
    ("Скромный", "не гонится за вниманием", "😇"),
    ("Злопамятный", "в его блокноте есть список", "😡"),
    ("Паникёр", "кричит «всё пропало!» первым", "🚨"),
    ("Ленивый", "движение — не его стихия", "🛋️"),
    ("Нервный", "дергается, даже когда всё спокойно", "😬"),
    ("Туповатый", "вопросы задаёт — но искренне", "🐑"),
    ("Завистливый", "хочет то же, что у других", "👀"),
    ("Эгоистичный", "ставит себя на первое место", "🙃"),
    ("Меланхоличный", "настроен на осеннюю волну", "🌙"),
    ("Философский", "спрашивает «зачем всё это?»", "🤔"),
    ("Хвастливый", "любит говорить о себе в третьем лице", "💪"),
    ("Слишком подозрительный", "даже чайник вызывает сомнения", "👀"),
    ("Романтичный", "смотрит на мир через сердечки", "🌅"),
    ("Циничный", "не верит в добрые намерения", "🧩"),
    ("Саркастичный", "говорит одно, а думает другое", "🗡️"),
    ("Добрый", "готов поделиться последним", "🧸"),
    ("Надёжный", "держит слово при любой погоде", "🛡️"),
    ("Сонный", "всегда в режиме перезагрузки", "😴"),
    ("Прямолинейный", "не умеет сглаживать углы", "🪓"),
    ("Безбашенный", "любит риск больше, чем стабильность", "🎢"),
    ("Мечтательный", "строит воздушные замки", "☁️"),
    ("Реалист", "не строит иллюзий", "🔍"),
    ("Педантичный", "всё по плану, и точка", "🧼"),
    ("Гиперактивный", "даже тень от него не успевает", "🦿"),
    ("Спонтанный", "планирует в последний момент", "💡"),
    ("Холодный", "эмоции — не его стиль", "🧊"),
    ("Тёплый", "излучает уют даже в словах", "🔥"),
    ("Целеустремлённый", "знает, куда идёт, и идёт туда", "🎯"),
    ("Амбициозный", "ставит высокую планку", "🌍"),
    ("Чудаковатый", "не такой как все — и горд этим", "🛸"),
    ("Игривый", "всегда найдет повод для шалости", "😼"),
    ("Злополучный", "проблемы липнут сами", "🪤"),
    ("Заботливый", "всегда узнает, как ты спал", "🍵"),
    ("Мстительный", "помнит всё... и когда вернётся", "📅"),
    ("Рассудительный", "не делает шаг без анализа", "📘"),
    ("Авантюрный", "любит рисковать ради интереса", "🎮"),
    ("Скептичный", "всегда ищет подвох", "🧐"),
    ("Залипательный", "трудно оторваться, когда он рядом", "🌀"),
    ("Творческий", "фантазия бьёт через край", "🎆"),
    ("Сладкоежка", "жизнь без шоколада — не жизнь", "🍰"),
    ("Любопытный", "ему всё интересно до мелочей", "🔎"),
    ("Молчаливый", "говорит глазами", "🤫"),
    ("Драматичный", "из мухи сделает трагедию", "🎭"),
    ("Самоироничный", "смеётся над собой искренне", "🪞"),
    ("Ловкий", "всегда выкрутится, даже из узла", "🤹"),
    ("Хитрый", "обойдёт проблему стороной", "🦊"),
    ("Забывчивый", "вечно ищет, что только что держал", "🧠❌"),
    ("Модный", "следит за трендами даже во сне", "🕶️"),
    ("Расслабленный", "даже в панике зевает", "🛀"),
    ("Бесстрашный", "идёт туда, где страшно всем", "🦇"),
    ("Стратегичный", "просчитывает на десять шагов вперёд", "♟️"),
    ("Остроумный", "его фразочки становятся мемами", "💬"),
    ("Терпеливый", "ждёт, когда кактус зацветёт", "⏳"),
    ("Забавный", "улыбка появляется сама", "🤣"),
    ("Неукротимый", "ни в клетку, ни в рамки", "🌪️"),
    ("Скромный", "держится в стороне, но делает много", "🙈"),
    ("Тайный", "о нём всегда остаются вопросы", "🎭"),
    ("Эмоциональный", "все чувства — на лице", "🌊"),
    ("Спокойный", "даже шторм его не колышет", "🐞"),
    ("Болтливый", "переходит на монолог после приветствия", "🗣️"),
    ("Тусовочный", "знает, где вечеринка ещё до начала", "🎉"),
    ("Романтичный циник", "любит, но без иллюзий", "🌹😏"),
    ("Чистюля", "тряпка — его лучший друг", "🧽"),
    ("Раздражительный", "даже тишина его бесит", "📶😠"),
    ("Фантазёр", "его мысли — как фильмы без бюджета", "📖✨"),
    ("Молчаливый стратег", "тихо строит империи", "🧊♟️"),
    ("Вечный студент", "учится, чтобы потом снова учиться", "📚"),
    ("Уверенный", "входит как хозяин", "🕴️"),
    ("Сомневающийся", "всё ещё решает, что надеть", "🤷"),
    ("Непреклонный", "не согнётся даже под давлением", "🪨"),
    ("Беспокойный", "в голове — вечный шторм", "🌀"),
    ("Везунчик", "удача всегда с ним на связи", "🍀"),
    ("Пофигист", "плывёт по течению — и кайфует", "🧘"),
    ("Обидчивый", "в обидах — чемпион мира", "📱💔"),
    ("Обаятельный", "улыбка — его суперсила", "😊"),
    ("Медлительный", "идёт, но красиво", "🐢"),
    ("Одержимый идеей", "не ест, пока не доделает", "🎯🔥"),
    ("Интуитивный", "угадает, что ты скажешь", "🔮🍕"),
    ("Занудный", "напоминает о правилах даже в выходной", "📏"),
    ("Сам себе праздник", "весёл без повода", "🎊"),
    ("Параноик", "и зеркало заклеил", "🎙️😳"),
    ("Фанатичный", "всё ради цели", "💣"),
    ("Приятный во всём", "хочется обнять просто так", "🌵❤️"),
    ("Эмпатичный", "плачет на чужих фильмах", "🤝💞"),
    ("Прагматик", "мечтает эффективно", "📈"),
    ("Апатичный", "даже новости не трогают", "🛌"),
    ("Обходительный", "даже спор ведёт вежливо", "🤗"),
    ("Идеалист", "мечтает о мире без ошибок", "🎬"),
    ("Экспрессивный", "эмоции — его язык", "👐"),
    ("Контролирующий", "не спит, пока не всё по плану", "🎛️"),
    ("Бунтарь", "делает по-своему — и точка", "🚫🔥"),
    ("Праздный", "в отдыхе — мастер", "🍹"),
    ("Честолюбивый", "видит цель — не видит препятствий", "🥇"),
    ("Переживательный", "волнуется даже за кота соседа", "🪐😟"),
    ("Быстро загорается", "одна мысль — и он уже в деле", "⚡🧠"),
    ("Бывает в облаках", "мечтает, даже стоя в пробке", "☁️👀"),
    ("Неунывающий", "смеётся сквозь трудности", "💪🙂"),
    ("Молчаливый философ", "молчит — но с смыслом", "🌌🤫"),
    ("Беспредельно добрый", "обнимет даже ёжика", "🐞🍞"),
]

# Изначальный текст команды /инфо с использованием HTML разметки
info_text = (
    "ℹ️ <b>Информация о боте:</b>\n\n"
    "🤖   <b>BotName</b> - observer_nautriks\n"
    "📅 <b>Версия:</b> 3.2\n"
    "👨‍💻 <b>Разработчик:</b> @nautriks\n"
    "\n"
    "<b>Основные функции:</b>\n"
    "- 🔔 Приветствие новых участников\n"
    "- 🚫 Модерация сообщений (цензура и муты)\n"
    "- 📢 Команды управления (бан, кик, мут)\n"
    "- 🧹 Удаление сообщений\n"
    "- 😂 Случайные анекдоты\n"
    "- 📊 Статистика участников (всего, онлайн, админы)\n"
    "- ⏰ Планирование напоминаний с выбором дня недели и времени\n"
    "\n"
    "<b>Дополнительные команды:</b>\n"
    "- 🎉 Реакция бота на каждое 150-е сообщение\n"
    "- 📩 Возможность отправлять сообщения в чат и личные от лица бота\n"
    "\n"
    "💡 Если у вас есть вопросы, обратитесь к администратору - <b>@nautriks</b>\n"
    "✨ Спасибо, что используете нашего бота!"
)

word_list = [
        "автомобиль", "банан", "велосипед", "грузовик", "дерево", "ежевика", "жираф", "зебра", "игрушка", "йогурт", "карандаш",
        "лампа", "молоко", "носорог", "облако", "пингвин", "радуга", "собака", "телефон", "улитка", "фламинго", "хомяк", "черепаха",
        "шоколад", "яблоко", "амфибия", "балкон", "ветер", "гитара", "двигатель", "елка", "жемчуг", "замок", "иглу", "капитан",
        "лифт", "маляр", "небо", "орел", "песок", "ромашка", "снег", "троллейбус", "улей", "фотоаппарат", "хлеб", "часовня",
        "шляпа", "ярлык", "акварель", "бабочка", "веник", "голубь", "дом", "елочка", "жук", "зайка", "игла", "календарь", "лиса",
        "медведь", "нефть", "обувь", "парус", "ракета", "солнце", "туча", "утюг", "фараон", "хрусталь", "чашка", "шарф", "яблоня",
        "автобус", "балерина", "вертолет", "горшок", "динамит", "еж", "желудь", "зеркало", "инструмент", "квадрат", "лобстер", "музыка",
        "носок", "олень", "потолок", "роза", "сокол", "трактор", "угол", "фонарь", "художник", "часы", "щенок", "якорь", "ананас", "бархат",
        "водопад", "груша", "деревня", "единорог", "жажда", "заметка"                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          "аквариум",
        "бензин", "вулкан", "глобус", "домино", "ежедневник", "журнал", "зонтик", "изумруд", "йога", "карета", "линия",
        "муравей", "наковальня", "осьминог", "плед", "рыцарь", "свеча", "тролль", "утка", "фокус", "химия", "цирк",
        "чемодан", "шапка", "юла", "авиатор", "берег", "ваза", "гигант", "дикобраз", "ежик", "журавль", "заморозка",
        "инопланетянин", "кактус", "лапша", "магнит", "нитка", "огурец", "пароход", "рыба", "салют", "такси", "удочка",
        "фото", "холст", "цветок", "чайка", "шуба", "ювелир", "антенна", "батарея", "винт", "город", "диск",
        "ежевичный", "железо", "золото", "индикатор", "казино", "лед", "мандарин", "нож", "охотник", "пицца", "робот",
        "сапог", "табурет", "униформа", "фонтан", "холм", "цикл", "человек", "шлем", "ягуар", "антидот", "бублик",
        "вишня", "гвоздь", "дуб", "ель", "жара", "змея", "инженер", "канат", "ледник", "маска", "нос", "океан",
        "пиджак", "рулетка", "сахар", "труба", "уголь", "ферма", "хребет", "циркуляция", "чернила", "щупальце",
        "яблочный", "акробат", "бита", "волна", "гроза", "дым", "ежик", "жокей", "загадка", "игра", "каблук",
        "ласточка", "меч", "налог", "орех", "порошок", "радио", "сапфир", "тележка", "улов", "фрукт", "хоккей",
        "циркуль", "чайник", "шпинат", "юрист", "астероид", "баклажан", "вентилятор", "гитара", "дело", "ежевика",
        "жилье", "зеленый", "иголка", "камень", "липа", "марка", "ножницы", "овраг", "пальма", "роса", "синтезатор",
        "транспорт", "упаковка", "флаг", "хлебушек", "циркуль", "чугун", "шпагат", "ягодник", "амулет", "бар", "водка",
        "гимнаст", "диван", "еженедельник", "жулик", "заговор", "игрушечный", "капелька", "лодка", "молния", "нота",
        "обезьяна", "перчатка", "руль", "сказка", "тень", "уран", "фон", "хвоя", "цена", "чистота", "шайба", "яблочко",
        "апельсин", "бумеранг", "воск", "глобус", "дубль", "ежиха", "жучок", "зебра", "изолента", "капот", "лук",
        "метла", "ножка", "огород", "пластик", "рыбалка", "сарай", "таз", "университет", "форум", "художница", "цвет",
        "чек", "штанга", "ядро", "арена", "борода", "восход", "горка", "джем", "ежик", "жираф", "запас", "иллюзия",
        "колесо", "лодка", "микрофон", "носки", "оправа", "платье", "робот", "самолет", "топор", "улей", "фрукты",
        "хобби", "цифра", "чай", "шашлык", "ягода", "аромат", "белка", "враг", "горизонт", "день", "ежик", "жена",
        "знамя", "интернет", "колодец", "лазер", "молния", "ножи", "оператор", "плов", "рубашка", "сокол", "торт",
        "утюг", "фотоаппарат", "хвост", "цветок", "череп", "шлем", "ястреб", "ананас", "банка", "вилка", "гонка",
        "дети", "еж", "жар", "заяц", "икра", "коляска", "лапа", "мышь", "ножка", "обруч", "перец", "рак", "сахар",
        "тюль", "утка", "футбол", "хвостик", "цветочек", "чемпион", "шорты", "ярмарка", "арбуз", "батон", "вино",
        "горох", "динозавр", "ежик", "живопись", "знак", "иллюзия", "книжка", "лягушка", "моряк", "нож", "обед",
        "пилот", "ручка", "соболь", "тюбик", "ухо", "фен", "хлеб", "цветы", "чайник", "шоссе", "якорь", "архив",
        "будильник", "виноград", "гриль", "душ", "ежик", "жест", "зима", "индюк", "кобра", "лампа", "металл", "нота",
        "обруч", "пирог", "рыбка", "собака", "труба", "улитка", "фейерверк", "хлопок", "цилиндр", "черешня", "шар",
        "яичница", "астроном", "банан", "вентиль", "горчица", "дракон", "ежик", "жилет", "зуб", "искатель", "класс",
        "лиса", "мёд", "нитка", "озеро", "пластинка", "роза", "сеть", "трава", "урок", "фанера", "хутор", "циркуль",
        "черепаха", "шляпка", "яблоко", "акула", "бассейн", "виски", "голова", "деньги", "ежик", "жизнь", "запах",
        "игрушка", "кисть", "локоть", "метр", "носок", "огонь", "птица", "рецепт", "сигнал", "точка", "ухо", "фильм",
        "хата", "цвет", "чек", "штора", "ягодка"
    ]

# Список доступных действий
available_operations = (
    "Список доступных действий:\n"
    "➕ Сложение: a + b\n"
    "➖ Вычитание: a - b\n"
    "✖️ Умножение: a * b\n"
    "➗ Деление с остатком: a / b\n"
    "➗ Деление нацело: a // b\n"
    "🔢 Остаток от деления: a % b\n"
    "⬆️ Возведение в степень: a ** b\n"
    "📐 Тригонометрия:\n"
    "   - sinX: синус угла X (в градусах)\n"
    "   - cosX: косинус угла X (в градусах)\n"
    "   - tgX: тангенс угла X (в градусах)\n"
    "   - ctgX: котангенс угла X (в градусах)\n\n"
    "Дополнительные таблицы:\n"
    "   - /таблица_умножения: таблица умножения (1-10)\n"
    "   - /таблица_степеней: таблица степеней (1-10)\n"
    "   - /таблица_квадратов: таблица квадратов (1-20)\n\n"
    "Введите пример в формате 'a <операция> b' или 'sinX', 'cosX', 'tgX', 'ctgX'"
    "\n\n*Для выхода из сессии введите повторно /математика"
)


@bot.message_handler(commands=['member'])
def get_member_id(message):
    try:
        # Извлекаем аргументы команды
        args = message.text.split()

        if len(args) != 2:
            bot.reply_to(message, "⚠️ Неверный формат команды. Используйте: /member @username")
            return

        username = args[1]

        # Проверяем, начинается ли username с '@'
        if not username.startswith('@'):
            bot.reply_to(message, "⚠️ Укажите пользователя в формате @username.")
            return

        # Убираем '@' для поиска в API Telegram
        username = username[1:]

        # Получаем список участников через get_chat_administrators
        chat_id = message.chat.id

        # Telegram API не предоставляет прямого метода для получения всех пользователей,
        # поэтому мы можем использовать admins или использовать кешированные данные
        # Попробуем получить пользователя через get_chat_member
        try:
            member_info = bot.get_chat_member(chat_id, username)
            user_id = member_info.user.id
            bot.send_message(chat_id, f"ID пользователя @{username}: {user_id}")
        except Exception as e:
            bot.reply_to(message, f"⚠️ Не удалось найти пользователя @{username} в чате. Возможно, он отсутствует.")
            return
    except Exception as e:
        bot.reply_to(message, f"⚠️ Произошла ошибка: {str(e)}")

@bot.message_handler(commands=list(interactions.keys()))
def handle_interaction(message):
    try:
        # Проверяем наличие упоминания пользователя
        if message.reply_to_message:
            target = message.reply_to_message.from_user.username or message.reply_to_message.from_user.first_name
            sender = message.from_user.username or message.from_user.first_name
            command = message.text.split()[0][1:]  # Получаем команду без "/"

            # Случайный результат (успех или неудача)
            result = random.choice(["success", "fail"])
            interaction_message = interactions[command][result].format(sender=sender, target=target)
            bot.send_message(message.chat.id, interaction_message)
        else:
            bot.reply_to(message, "⛔ Команду нужно использовать в ответ на сообщение участника!")
    except KeyError:
        bot.reply_to(message, "⛔ Неправильная команда взаимодействия!")

# Функция для получения упомянутого пользователя
def get_mentioned_user(message):
    if message.reply_to_message:
        return message.reply_to_message.from_user
    if message.entities:
        for entity in message.entities:
            if entity.type == 'mention':
                username = message.text[entity.offset + 1: entity.offset + entity.length]
                try:
                    user_info = bot.get_chat_member(message.chat.id, username)
                    return user_info.user
                except:
                    return None
    return None

# Команда /математика — активирует сессию вычислений
@bot.message_handler(commands=['математика'])
def start_math_session(message):
    user_id = message.from_user.id

    # Если пользователь уже активировал сессию, сбрасываем её
    if user_id in active_sessions:
        active_sessions.remove(user_id)
        bot.send_message(message.chat.id, "🔄 Сессия сброшена. Введите /математика снова для начала нового сеанса.")
    else:
        active_sessions.add(user_id)
        bot.send_message(message.chat.id, available_operations)

# Команда /таблица_умножения
@bot.message_handler(commands=['таблица_умножения'])
def multiplication_table(message):
    table = "📊 Таблица умножения (1-10):\n"
    for i in range(1, 11):
        row = " | ".join([f"{i}×{j}={i * j}" for j in range(1, 11)])
        table += row + "\n"
    bot.send_message(message.chat.id, f"<pre>{table}</pre>", parse_mode="HTML")

# Команда /таблица_степеней
@bot.message_handler(commands=['таблица_степеней'])
def powers_table(message):
    table = "📊 Таблица степеней (1-10):\n"
    for i in range(1, 11):
        row = " | ".join([f"{i}^{j}={i ** j}" for j in range(1, 11)])
        table += row + "\n"
    bot.send_message(message.chat.id, f"<pre>{table}</pre>", parse_mode="HTML")

# Команда /таблица_квадратов
@bot.message_handler(commands=['таблица_квадратов'])
def squares_table(message):
    table = "📊 Таблица квадратов (1-20):\n"
    for i in range(1, 21):
        table += f"{i}² = {i ** 2}\n"
    bot.send_message(message.chat.id, f"<pre>{table}</pre>", parse_mode="HTML")

# Обработчик сообщений с математическими выражениями
@bot.message_handler(func=lambda message: message.from_user.id in active_sessions)
def solve_math_expression(message):
    expression = message.text.strip()

    # Проверяем тригонометрические функции
    trig_match = re.match(r'(sin|cos|tg|ctg)(-?\d+)', expression)
    if trig_match:
        try:
            func, angle = trig_match.groups()
            angle = float(angle)

            # Преобразуем градусы в радианы
            radians = math.radians(angle)

            # Выполняем вычисление
            if func == 'sin':
                result = math.sin(radians)
            elif func == 'cos':
                result = math.cos(radians)
            elif func == 'tg':
                result = math.tan(radians)
            elif func == 'ctg':
                result = 1 / math.tan(radians) if math.tan(radians) != 0 else '∞'  # Проверка деления на 0

            bot.send_message(message.chat.id, f"📊 Результат {func}{angle}: {result:.6f}")
        except Exception:
            bot.reply_to(message, "⚠️ Ошибка в вычислении тригонометрической функции.")
        return

    # Проверка на арифметические выражения
    match = re.match(r'(-?\d+)\s*([+\-*/%//**]+)\s*(-?\d+)', expression)
    if not match:
        bot.reply_to(message, "⚠️ Некорректный формат. Введите пример в формате 'a <операция> b' или 'sinX', 'cosX', 'tgX', 'ctgX'")
        return

    # Извлекаем числа и операцию
    a = int(match.group(1))
    operator = match.group(2)
    b = int(match.group(3))

    # Выполняем вычисление
    try:
        if operator == '+':
            result = a + b
        elif operator == '-':
            result = a - b
        elif operator == '*':
            result = a * b
        elif operator == '/':
            result = a / b
        elif operator == '//':
            result = a // b
        elif operator == '%':
            result = a % b
        elif operator == '**':
            result = a ** b
        else:
            bot.reply_to(message, "⚠️ Неизвестная операция.")
            return

        bot.send_message(message.chat.id, f"📊 Результат: {result}")
    except ZeroDivisionError:
        bot.reply_to(message, "⚠️ Ошибка: Деление на ноль невозможно.")


@bot.message_handler(commands=['характеристика'])
def send_characteristic(message):
    try:
        # Проверка, упомянут ли участник через ответ на сообщение
        if message.reply_to_message:
            target = message.reply_to_message.from_user.username or message.reply_to_message.from_user.first_name
        elif len(message.text.split()) > 1:
            target = message.text.split()[1]  # Получаем указанный username
        else:
            bot.reply_to(message, "⛔ Укажите участника с помощью упоминания или ответа на его сообщение!")
            return

        # Генерация 3 случайных характеристик
        selected_characteristics = random.sample(characteristics, 3)

        response_text = f"""
🗄️ Шкаф характеристик 🗄️
┌─────────────────────────┐
│  Участник: {target}    │
├─────────────────────────┤
│     {selected_characteristics[0][0]} {selected_characteristics[0][2]} 
│     — {selected_characteristics[0][1]} 
├─────────────────────────┤
│     {selected_characteristics[1][0]} {selected_characteristics[1][2]} 
│     — {selected_characteristics[1][1]} 
├─────────────────────────┤
│     {selected_characteristics[2][0]} {selected_characteristics[2][2]} 
│     — {selected_characteristics[2][1]} 
└─────────────────────────┘
        """

        bot.send_message(message.chat.id, response_text)

    except Exception as e:
        bot.reply_to(message, f"⛔ Ошибка: {str(e)}")


# Приветственное сообщение для новых участников
@bot.message_handler(content_types=['new_chat_members'])
def welcome_new_member(message):
    for new_user in message.new_chat_members:
        bot.send_message(message.chat.id, f"👋 Добро пожаловать, {new_user.first_name}!")

# Функция для фильтрации плохих слов
@bot.message_handler(func=lambda message: any(bad_word in message.text.lower() for bad_word in BAD_WORDS))
def bad_word_detected(message):
    bot.delete_message(message.chat.id, message.message_id)

    # Мьют пользователя на 1 час
    bot.restrict_chat_member(
        message.chat.id,
        message.from_user.id,
        until_date=datetime.now() + timedelta(hours=1),
        can_send_messages=False
    )

    # Создание кнопки для снятия мута
    unmute_button = InlineKeyboardMarkup().add(
        InlineKeyboardButton("Снять мут", callback_data=f"unmute_{message.from_user.id}")
    )

    # Экранирование имени пользователя для корректного использования в HTML
    user_mention = f"<a href='tg://user?id={message.from_user.id}'>{html.escape(message.from_user.first_name)}</a>"

    # Сообщение о мьюте с кнопкой "Снять мут"
    bot.send_message(
        message.chat.id,
        f"🔇 {user_mention}, вы временно замолчаны на 1 час за плохие слова!",
        parse_mode='HTML',
        reply_markup=unmute_button
    )

# Обработчик для снятия мута с проверкой прав администратора
@bot.callback_query_handler(func=lambda call: call.data.startswith("unmute_"))
def unmute_user(call):
    user_id = int(call.data.split('_')[1])  # Получаем ID пользователя из callback_data

    # Проверяем статус пользователя, который нажал на кнопку
    user_status = bot.get_chat_member(call.message.chat.id, call.from_user.id).status

    if user_status in ['administrator', 'creator']:
        try:
            # Снимаем ограничения с пользователя
            bot.restrict_chat_member(
                call.message.chat.id,
                user_id,
                can_send_messages=True,
                can_send_media_messages=True,
                can_send_other_messages=True,
                can_add_web_page_previews=True
            )

            # Сообщаем о снятии мута
            bot.send_message(call.message.chat.id,
                             f"🔊 Пользователь был размьючен.")

        except Exception as e:
            bot.send_message(call.message.chat.id, f"❗ Не удалось снять мут: {e}")
    else:
        # Сообщение для неадминистраторов
        bot.answer_callback_query(call.id, "Убери руки, надо иметь права", show_alert=True)

# Команда для мьюта пользователя
@bot.message_handler(commands=['мут'])
def mute_user(message):
    if message.reply_to_message:
        user_status = bot.get_chat_member(message.chat.id, message.from_user.id).status
        if user_status in ['administrator', 'creator']:
            try:
                command_parts = message.text.split()
                if len(command_parts) < 3:
                    bot.reply_to(message, "❗ Укажите корректное время и причину. Пример: /мут 1 час Флуд.")
                else:
                    duration = int(command_parts[1])  # время мута
                    reason = ' '.join(command_parts[2:])  # причина
                    mute_duration = timedelta(hours=duration)

                    # Мьют пользователя
                    bot.restrict_chat_member(
                        message.chat.id,
                        message.reply_to_message.from_user.id,
                        until_date=datetime.now() + mute_duration
                    )

                    # Создание кнопки для снятия мута
                    unmute_button = InlineKeyboardMarkup().add(
                        InlineKeyboardButton("Снять мут",
                                             callback_data=f"unmute_{message.reply_to_message.from_user.id}")
                    )

                    # Экранирование имени пользователя для корректного использования в HTML
                    user_mention = f"<a href='tg://user?id={message.reply_to_message.from_user.id}'>{html.escape(message.reply_to_message.from_user.first_name)}</a>"

                    # Отправка сообщения о мьюте с кнопкой "Снять мут"
                    bot.send_message(
                        message.chat.id,
                        f"🔇 Пользователь: {user_mention}\nПолучил мут на {duration} час(ов).\nПричина: {reason}.",
                        parse_mode='HTML',
                        reply_markup=unmute_button
                    )
            except Exception as e:
                bot.reply_to(message, f"❗ Произошла ошибка: {e}")
        else:
            bot.reply_to(message, "❗ У вас нет прав для выполнения этой команды.")
    else:
        bot.reply_to(message, "❗ Используйте команду ответом на сообщение, чтобы замьютить участника.")


# Обработчик для снятия мута с проверкой прав администратора
@bot.callback_query_handler(func=lambda call: call.data.startswith("unmute_"))
def unmute_user(call):
    user_id = int(call.data.split('_')[1])  # Получаем ID пользователя из callback_data

    # Проверяем статус пользователя, который нажал на кнопку
    user_status = bot.get_chat_member(call.message.chat.id, call.from_user.id).status

    if user_status in ['administrator', 'creator']:
        try:
            # Снимаем ограничения с пользователя
            bot.restrict_chat_member(
                call.message.chat.id,
                user_id,
                can_send_messages=True,
                can_send_media_messages=True,
                can_send_other_messages=True,
                can_add_web_page_previews=True
            )

            # Сообщаем о снятии мута
            bot.send_message(call.message.chat.id, f"🔊 Пользователь был размьючен.")

        except Exception as e:
            bot.send_message(call.message.chat.id, f"❗ Не удалось снять мут: {e}")
    else:
        # Сообщение для неадминистраторов
        bot.answer_callback_query(call.id, "Убери руки, надо иметь права", show_alert=True)


# Путь к папке, где будет сохраняться база данных
db_folder = r"C:\Users\Ш\OneDrive\Desktop\tgbot\tgbots"  # Используем raw-строку для корректной обработки пути

# Убедимся, что папка существует
os.makedirs(db_folder, exist_ok=True)

# Путь к базе данных
db_path = os.path.join(db_folder, "bans.db")

# Команда для бана пользователя
@bot.message_handler(commands=['бан'])
def ban_user(message):
    # Проверка, что команда использована ответом на сообщение
    if not message.reply_to_message:
        bot.reply_to(message, "❗ Используйте команду ответом на сообщение, чтобы забанить участника.")
        return

    # Проверка прав администратора
    user_status = bot.get_chat_member(message.chat.id, message.from_user.id).status
    if user_status not in ['administrator', 'creator']:
        bot.reply_to(message, "❗ У вас нет прав для выполнения этой команды.")
        return

    # Обработка аргументов команды
    command_parts = message.text.split()
    if len(command_parts) < 3:
        bot.reply_to(message, "❗ Укажите корректное время (в часах) и причину. Пример: /бан 1 Спам.")
        return

    try:
        duration = command_parts[1]
        reason = ' '.join(command_parts[2:])
        target_user = message.reply_to_message.from_user

        # Бан пользователя
        bot.kick_chat_member(message.chat.id, target_user.id)

        # Запись в базу данных
        with sqlite3.connect(db_path) as conn:
            cursor = conn.cursor()
            cursor.execute('''
                CREATE TABLE IF NOT EXISTS banned_users (
                    id INTEGER PRIMARY KEY AUTOINCREMENT,
                    user_id INTEGER NOT NULL,
                    username TEXT,
                    reason TEXT,
                    ban_date TIMESTAMP DEFAULT CURRENT_TIMESTAMP
                )
            ''')
            cursor.execute(
                "INSERT INTO banned_users (user_id, username, reason) VALUES (?, ?, ?)",
                (target_user.id, target_user.username or '', reason)
            )

        # Кнопка разбан
        unban_button = InlineKeyboardMarkup().add(
            InlineKeyboardButton("Разбанить", callback_data=f"unban_{target_user.id}")
        )

        # Упоминание
        user_mention = f"<a href='tg://user?id={target_user.id}'>{html.escape(target_user.first_name)}</a>"

        bot.send_message(
            message.chat.id,
            f"🚫 Пользователь: {user_mention}\nБыл забанен на {duration} часов.\nПричина: {reason}.",
            parse_mode='HTML',
            reply_markup=unban_button
        )

    except Exception as e:
        bot.reply_to(message, f"❗ Произошла ошибка: {e}")


# Обработчик кнопки для разбана
@bot.callback_query_handler(func=lambda call: call.data.startswith("unban_"))
def unban_user(call):
    user_id = int(call.data.split('_')[1])
    user_status = bot.get_chat_member(call.message.chat.id, call.from_user.id).status

    if user_status in ['administrator', 'creator']:
        try:
            bot.unban_chat_member(call.message.chat.id, user_id)
            bot.send_message(call.message.chat.id, f"🔓 Пользователь был разбанен.")
        except Exception as e:
            bot.send_message(call.message.chat.id, f"❗ Не удалось разбанить пользователя: {e}")
    else:
        bot.answer_callback_query(call.id, "Убери руки, надо иметь права", show_alert=True)



# Функция для создания инлайн-кнопок
def create_inline_keyboard():
    keyboard = types.InlineKeyboardMarkup()
    button = types.InlineKeyboardButton("Мыслители", callback_data="thinkers")
    keyboard.add(button)
    return keyboard

# Команда /инфо
@bot.message_handler(commands=['инфо'])
def send_info(message):
    try:
        bot.send_message(message.chat.id, info_text, parse_mode='HTML', reply_markup=create_inline_keyboard())
    except Exception as e:
        print(f"Ошибка при отправке информации: {e}")

# Обработчик нажатия инлайн-кнопки "Мыслители"
@bot.callback_query_handler(func=lambda call: call.data == "thinkers")
def thinkers(call):
    new_text = "<b>Помогали придумывать идеи:</b>\n@VoriXq\n@Katerina_blsh\n"
    new_keyboard = types.InlineKeyboardMarkup()
    back_button = types.InlineKeyboardButton("Вернуть информацию о боте", callback_data="return_info")
    new_keyboard.add(back_button)

    try:
        bot.edit_message_text(chat_id=call.message.chat.id, message_id=call.message.message_id, text=new_text, reply_markup=new_keyboard, parse_mode='HTML')
    except Exception as e:
        print(f"Ошибка при редактировании сообщения о мыслителях: {e}")

# Обработчик нажатия инлайн-кнопки "Вернуть информацию о боте"
@bot.callback_query_handler(func=lambda call: call.data == "return_info")
def return_info(call):
    try:
        bot.edit_message_text(chat_id=call.message.chat.id, message_id=call.message.message_id, text=info_text, reply_markup=create_inline_keyboard(), parse_mode='HTML')
    except Exception as e:
        print(f"Ошибка при возврате информации о боте: {e}")


# Команда "хелп" для отправки списка команд с кнопками
@bot.message_handler(commands=['хелп'])
def user_help(message):
    markup = types.InlineKeyboardMarkup()

    # Кнопки для взаимодействия
    interaction_buttons = [
        types.InlineKeyboardButton("Взаимодействие", callback_data='interaction_commands'),
        types.InlineKeyboardButton("Другие команды", callback_data='other_commands'),
    ]

    markup.add(*interaction_buttons)

    bot.send_message(message.chat.id, "📜 Выберите группу команд:", reply_markup=markup)


# Обработчик нажатий на кнопки
@bot.callback_query_handler(func=lambda call: call.data in ['interaction_commands', 'other_commands'])
def send_help(call):
    if call.data == 'interaction_commands':
        help_text = """
\t\tДля использования нужно <u>Ответить</u> на сообщение
        
📌 <b>Команды взаимодействия:</b>
1. /убить - Попытка убить пользователя.
2. /поцеловать - Поцеловать пользователя.
3. /обнять - Обнять пользователя.
4. /ударить - Ударить пользователя.
5. /заразить - Заразить пользователя.
6. /сжечь - Поджечь пользователя.
7. /рассмешить - Рассмешить пользователя.
8. /застрелить - Расстрелять пользователя.
9. /кастрировать - Лишить достоинсва пользователя.
10. /взбесить - Взбесить пользователя.
11. /испугать - Испугать пользователя.
12. /похвалить - Похвалить пользователя
13. /подарить - Подарить подарок пользователю.
14. /пнуть - Дать под зад пользователю.
15. /клоун - Сделать из пользователя клоуна.
"""
    else:
        help_text = """
📌 <b>Другие команды:</b>
1. /анекдот - Получить случайный анекдот.
2. /шар - Магический шар ответит на ваш вопрос.
3. /комплимент - Получить комплимент.
4. /инфо - Узнать информацию о боте.
5. /характеристика @пользователь- Разложить по полочкам пользователя.
6. /найти - Найти что-то в гугле.
7. /сообщения - Отправляет кол-во отправленных сообщений в чат.
8. /актив - Отправляет ТОП пользователей чата.
9. /совместимость @1 @2 - узнать совместимость двух участников.
10. /виселица - игра в виселицу
11. /математика - активирует сессию вычислений.
12. /песня - найти песню на ютубе.
13. /спам - отправляет случайные сообщения вам в лс.
14. /погода - узнать погоду в городе.
15. /температура - узнать температуру рядом с вами (использовавать только в личных сообщениях с ботом)
16. /упоминуть - упомянуть пользователя в чате.
17. /myid - узнать свой ID.
18. /перевести - перевести текст на другой язык.
19. /пинг - проверить задержку бота.
"""

    # Ответ на нажатие кнопки
    bot.send_message(call.message.chat.id, help_text, parse_mode='HTML')
    bot.answer_callback_query(call.id)  # Убираем индикатор загрузки


# 8. Калькулятор любви
@bot.message_handler(commands=['совместимость'])
def love_calculator(message):
    try:
        names = message.text.split()[1:]
        if len(names) != 2:
            bot.reply_to(message, "Пожалуйста, введите два имени: /совместимость Имя1 Имя2")
            return
        love_score = random.randint(0, 100)
        bot.reply_to(message, f"Совместимость между {names[0]} и {names[1]}: {love_score}% {heart_emoji}")
    except IndexError:
        bot.reply_to(message, "Использование: /совместимость Имя1 Имя2")

# Команда "удалить" для удаления сообщений
@bot.message_handler(commands=['удалить'])
def delete_messages(message):
    if not is_admin(message.chat.id, message.from_user.id):
        bot.send_message(message.chat.id, "⚠️ У вас нет прав для выполнения этой команды!")
        return
    args = message.text.split()
    if len(args) < 2 or not args[1].isdigit():
        bot.send_message(message.chat.id, "❗ Неверный формат команды. Используйте: /удалить <количество>")
        return

    count = int(args[1])
    chat_id = message.chat.id
    message_ids = [message.message_id - i for i in range(count)]

    for msg_id in message_ids:
        try:
            bot.delete_message(chat_id, msg_id)
        except Exception as e:
            bot.send_message(chat_id, f"❌ Не удалось удалить сообщение: {str(e)}")


# Переменные для игры
current_word = random.choice(word_list)
guessed_letters = []
attempts = 0
max_attempts = 6
is_game_active = False  # Переменная для отслеживания состояния игры
games = {}

# Команда для начала игры
@bot.message_handler(commands=['виселица'])
def start_hangman(message):
    chat_id = message.chat.id
    current_word = random.choice(word_list).lower()
    games[chat_id] = {
        'current_word': current_word,
        'guessed_letters': [],
        'attempts': 0,
        'is_game_active': True
    }
    hidden_word = " ".join(["_" for _ in current_word])
    bot.reply_to(message, f"Игра 'Виселица' началась! 🎉\nСлово: {hidden_word}")

# Обработка ввода одной буквы
@bot.message_handler(func=lambda msg: msg.text and len(msg.text) == 1 and msg.text.isalpha())
def hangman_game(message):
    chat_id = message.chat.id
    game = games.get(chat_id)

    if not game or not game['is_game_active']:
        return  # Игнорировать, если нет активной игры

    letter = message.text.lower()

    if letter in game['guessed_letters']:
        bot.reply_to(message, f"Ты уже угадывал эту букву: {letter}")
        return

    game['guessed_letters'].append(letter)

    if letter in game['current_word']:
        hidden_word = " ".join([char if char in game['guessed_letters'] else "_" for char in game['current_word']])
        if "_" not in hidden_word:
            game['is_game_active'] = False
            bot.reply_to(message, f"Поздравляю! Ты угадал слово: {game['current_word']} 🎉")
        else:
            bot.reply_to(message, f"Угадал букву! {hidden_word}")
    else:
        game['attempts'] += 1
        if game['attempts'] >= max_attempts:
            game['is_game_active'] = False
            bot.reply_to(message, f"Ты проиграл! Слово было: {game['current_word']} 💀")
        else:
            bot.reply_to(message, f"Нет такой буквы! Осталось {max_attempts - game['attempts']} попыток.")


@bot.message_handler(commands=list(interactions.keys()))
def handle_interaction(message):
    print(f"[DEBUG] Получено сообщение: {message.text}")
    print(f"[DEBUG] reply_to_message: {message.reply_to_message}")

    try:
        # Проверяем, является ли сообщение ответом на другое
        if message.reply_to_message:
            target_user = message.reply_to_message.from_user
            # Убедимся, что это не ответ на сообщение самого бота
            if target_user.id == bot.get_me().id:
                print("[DEBUG] Это ответ на сообщение бота, пропускаем.")
                bot.reply_to(message, "⛔ Невозможно использовать команду в ответ на сообщение бота!")
                return

            # Получаем имена пользователей
            target = target_user.username or target_user.first_name
            sender = message.from_user.username or message.from_user.first_name
            command = message.text.split()[0][1:]  # убираем "/"

            # Проверка на существование команды
            if command not in interactions:
                bot.reply_to(message, "⛔ Неправильная команда взаимодействия!")
                return

            # Выбираем случайный результат
            result = random.choice(["success", "fail"])
            interaction_message = interactions[command][result].format(sender=sender, target=target)

            print(f"[DEBUG] Отправляем сообщение: {interaction_message}")
            bot.send_message(message.chat.id, interaction_message)

        else:
            print("[DEBUG] Не было ответа на сообщение.")
            bot.reply_to(message, "⛔ Команду нужно использовать в ответ на сообщение участника!")

    except Exception as e:
        print(f"[ERROR] Произошла ошибка: {e}")
        bot.reply_to(message, "⚠️ Произошла ошибка при выполнении команды.")



#Команда анекдота
@bot.message_handler(commands=['анекдот'])
def send_joke(message):
    try:
        response = requests.get("https://www.anekdot.ru/random/anekdot/")
        soup = BeautifulSoup(response.text, 'html.parser')
        joke = soup.find('div', class_='text').text.strip()
        bot.send_message(message.chat.id, f"😂 Анекдот:\n\n{joke}")
    except Exception as e:
        bot.send_message(message.chat.id, "❌ Не удалось получить анекдот. Попробуйте позже.")


# Команда для получения списка пользователей, админов и онлайн участников
@bot.message_handler(commands=['список'])
def user_list(message):
    chat_id = message.chat.id

    # Получаем количество всех участников
    try:
        members_count = bot.get_chat_members_count(chat_id)
    except Exception as e:
        bot.send_message(chat_id, f"❌ Не удалось получить количество участников: {str(e)}")
        return

    # Получаем список администраторов
    try:
        admins = bot.get_chat_administrators(chat_id)
        admin_count = len(admins)
        # В Telegram API нет явного признака того, что администратор онлайн, поэтому "онлайн" будет отсутствовать
        online_members = sum(1 for admin in admins if admin.status == 'online')
    except Exception as e:
        bot.send_message(chat_id, f"❌ Не удалось получить информацию об администраторах: {str(e)}")
        return

    bot.send_message(chat_id,
                     f"👥 Всего участников: {members_count}\n👤 Администраторов: {admin_count}\n")


# Команда: Помощь для администраторов
@bot.message_handler(commands=['ахелп'])
def admin_help(message):
    try:
        print(f"Получение статуса для user_id={message.from_user.id} в чате {message.chat.id}")
        
        chat_member = bot.get_chat_member(message.chat.id, message.from_user.id)
        user_status = chat_member.status  
        
        print(f"Статус пользователя: {user_status}")  # Лог в консоль

        if user_status in ['administrator', 'creator']:
            bot.reply_to(
                        message,
                                "🛠️ <b>*Помощь для администраторов:*</b>\n\n"
                                "1️⃣ /бан &lt;время&gt; &lt;причина&gt; - Заблокировать пользователя на определенное время.\n"
                                "2️⃣ /мут &lt;время&gt; &lt;причина&gt; - Выдать мут пользователю на указанное время.\n"
                                "3️⃣ /удалить &lt;количество сообщений&gt; - Удалить указанное количество сообщений.\n"
                                "4️⃣ /отлож - Создать отложенное сообщение.\n"
                                "5️⃣ /список-отлож - Посмотреть список отложенных сообщений.",
                                parse_mode='HTML'
                    )
        else:
            bot.reply_to(message, "❗ У вас нет прав для использования этой команды.")

    except Exception as e:
        error_message = f"❗ Ошибка: {e}"
        print(error_message)  # Вывод в консоль
        bot.reply_to(message, error_message)  # Вывод ошибки в чат



# Подключение к базе данных и создание таблицы для логов
conn = sqlite3.connect('bot_logs.db', check_same_thread=False)
cursor = conn.cursor()

cursor.execute('''
CREATE TABLE IF NOT EXISTS logs (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    sender_username TEXT,
    sender_id INTEGER,
    recipient_username TEXT,
    recipient_id INTEGER,
    message TEXT,
    timestamp DATETIME DEFAULT CURRENT_TIMESTAMP
)
''')
conn.commit()

# Словарь для кеша пользователей
user_cache = {}


# Команда для отправки личного сообщения через бота
@bot.message_handler(commands=['лс'])
def send_private_message(message):
    try:
        # Извлекаем аргументы команды
        args = message.text.split(maxsplit=2)
        if len(args) < 3:
            bot.reply_to(message, "⚠️ Неверный формат команды. Используйте: /лс @username <текст>")
            return

        sender_username = message.from_user.username
        sender_id = message.from_user.id
        username = args[1]
        text_to_send = args[2]

        # Проверяем, начинается ли username с '@'
        if not username.startswith('@'):
            bot.reply_to(message, "⚠️ Укажите пользователя в формате @username.")
            return

        # Убираем '@' для поиска в API Telegram
        username = username[1:]

        # Попробуем найти пользователя в кеше
        if username in user_cache:
            user_id = user_cache[username]
            bot.send_message(user_id, text_to_send)

            # Записываем лог в базу данных
            log_message(sender_username, sender_id, username, user_id, text_to_send)

            # Удаляем сообщение команды из чата
            bot.delete_message(message.chat.id, message.message_id)

            # Отправляем уведомление отправителю в ЛС
            bot.send_message(sender_id, f"✅ Ваше сообщение было успешно отправлено пользователю @{username}.")

            return

        # Получаем информацию о пользователе через API
        try:
            chat_member = bot.get_chat_member(message.chat.id, message.reply_to_message.from_user.id)
            user_id = chat_member.user.id
        except Exception as e:
            bot.reply_to(message, f"⚠️ Не удалось найти пользователя @{username}. Попробуйте позже.")
            return

        # Сохраняем ID пользователя в кеш
        user_cache[username] = user_id

        # Отправляем сообщение
        bot.send_message(user_id, text_to_send)

        # Записываем лог в базу данных
        log_message(sender_username, sender_id, username, user_id, text_to_send)

        # Удаляем сообщение команды из чата
        bot.delete_message(message.chat.id, message.message_id)

        # Отправляем уведомление отправителю в ЛС
        bot.send_message(sender_id, f"✅ Ваше сообщение было успешно отправлено пользователю @{username}.")

    except Exception as e:
        bot.reply_to(message, f"⚠️ Ошибка при отправке сообщения: {str(e)}")


# Функция для записи логов в базу данных
def log_message(sender_username, sender_id, recipient_username, recipient_id, message_text):
    cursor.execute('''
        INSERT INTO logs (sender_username, sender_id, recipient_username, recipient_id, message, timestamp)
        VALUES (?, ?, ?, ?, ?, CURRENT_TIMESTAMP)
    ''', (sender_username, sender_id, recipient_username, recipient_id, message_text))
    conn.commit()


# Команда /логи для администраторов
@bot.message_handler(commands=['логи'])
def show_logs(message):
    # Проверяем, является ли пользователь администратором
    chat_id = message.chat.id
    user_id = message.from_user.id
    if not is_admin(chat_id, user_id):
        bot.reply_to(message, "⚠️ У вас нет прав администратора для этой команды.")
        return

    # Получаем все логи из базы данных
    cursor.execute('SELECT sender_username, recipient_username, message, timestamp FROM logs')
    logs = cursor.fetchall()

    if not logs:
        bot.send_message(chat_id, "📄 Логов пока нет.")
        return

    log_messages = "📄 <b>Логи сообщений:</b>\n\n"
    for log in logs:
        log_messages += f"Отправитель: @{log[0]}\nПолучатель: @{log[1]}\nСообщение: {log[2]}\nДата: {log[3]}\n\n"

    bot.send_message(chat_id, log_messages, parse_mode='HTML')


# Функция для проверки, является ли пользователь администратором
def is_admin(chat_id, user_id):
    member = bot.get_chat_member(chat_id, user_id)
    return member.status in ['administrator', 'creator']


# Команда /спам для инициации спам-рассылки
@bot.message_handler(commands=['спам'])
def request_spam_confirmation(message):
    user_id = message.from_user.id
    chat_id = message.chat.id

    # Создаем инлайн-кнопки для подтверждения
    markup = types.InlineKeyboardMarkup()
    button_confirm = types.InlineKeyboardButton("Подтвердить", callback_data=f"confirm_spam_{user_id}")
    button_cancel = types.InlineKeyboardButton("Отменить", callback_data=f"cancel_spam_{user_id}")
    markup.add(button_confirm, button_cancel)

    # Отправляем сообщение с запросом подтверждения
    bot.send_message(chat_id, "Вы уверены, что хотите начать спам? Подтвердите действие.", reply_markup=markup)

# Обработчик нажатий инлайн-кнопок для подтверждения спама
@bot.callback_query_handler(func=lambda call: call.data.startswith('confirm_spam') or call.data.startswith('cancel_spam'))
def handle_spam_confirmation(call):
    user_id = call.from_user.id
    chat_id = call.message.chat.id
    command_user_id = int(call.data.split('_')[-1])

    # Проверяем, что нажимает тот же пользователь, который вызывал команду
    if user_id != command_user_id:
        bot.answer_callback_query(call.id, "Только пользователь, вызвавший команду, может подтвердить действие.")
        return

    if call.data.startswith('confirm_spam'):
        # Подтверждение - начинаем спам
        bot.answer_callback_query(call.id, "Вы подтвердили спам. Спам начнется в ЛС.")
        bot.send_message(chat_id, "✅ Спам начат! Сообщения будут отправляться каждую 1 секунду в ЛС.")

        # Отправляем сообщение в ЛС пользователя с кнопкой "Остановить спам"
        markup = types.ReplyKeyboardMarkup(resize_keyboard=True)
        button_stop = types.KeyboardButton("Остановить спам")
        markup.add(button_stop)
        bot.send_message(user_id, "Спам начался! Чтобы остановить, нажмите кнопку ниже.", reply_markup=markup)

        # Запуск спама в ЛС пользователя
        spam_status[user_id] = True
        threading.Thread(target=spam_user, args=(user_id,)).start()

    elif call.data.startswith('cancel_spam'):
        # Отмена спама
        bot.answer_callback_query(call.id, "Вы отменили спам.")
        bot.send_message(chat_id, "❌ Спам был отменен.")

# Функция для отправки спам-сообщений
def spam_user(user_id):
    random_spam_messages = [
        ".", "..", "...", "Привет", "Как дела?", "Спам", "😂", "😜", "!", "💬", "Что нового?", "?", "!!!",
        "Сегодня отличный день", "123", "Хаха", "Ты тут?", "Все ок?", "😁", "👋", "🤔", "🤖", "🌞", "🌚", "⭐",
        "Играешь в игры?", "Смотришь фильмы?", "😉", "😎", "Ты занят?", "Чем занят?", "А ты?", "Мяу", "Гав",
        "Мир", "Любовь", "Хорошего дня", "Ты в сети?", "Что происходит?", "🐱", "🐶", "🐍", "Кто ты?", "Что ты?",
        "Зачем?", "Кто?", "Ты меня слышишь?", "💡", "🔥", "🌪", "❄", "🍔", "🍕", "Ты голоден?", "Я голоден...",
        "Прогулка?", "🏃", "🚶", "💼", "🏡", "Выйдешь?", "Кто там?", "Ого", "Ничего себе", "😱", "Да ты что!",
        "Ого-го!", "Ты серьезно?", "А где ты?", "На работе?", "В отпуске?", "Кто?", "Что?", "Зачем?",
        "Почему?", "Как так?", "Нереально!", "😮", "😯", "😲", "🙀", "Ты шутишь?", "Вау", "😋", "😐", "😴",
        "🤯", "Ты крутой!", "💪", "Сила", "Бот крут", "Готов к чему-то?", "Бот ждет", "Ты лучший", "👍", "🔥",
        "🥇", "Занимайся спортом", "Любишь сладкое?", "Кофе или чай?", "Пора на отдых", "🎬", "Ты умный",
        "Какой план?", "🕰️", "Ты в порядке?", "😊", "😉", "Мир и счастье", "Жду ответа", "🤖", "🤖", "Ты странный", "😜",
        "💀", "👻", "🔔", "🔕", "🤐", "🎃", "✨", "🌈", "🛌", "🐾", "Бегу к тебе", "Я тебя вижу", "👀", "Где ты был?",
        "Покажи!", "😎", "Это секрет", "💣", "🏆", "Где ответ?", "Я жду", "Говори", "🎲", "🎭", "🎮", "🐺", "🐷",
        "🍫", "🚀", "🥶", "Сюрприз", "🧐", "Что ты хочешь?", "🔥", "💦", "⚡", "🌋", "💨", "Гроза", "Солнце", "Море",
        "Река", "🏞️", "Мир без границ", "Ты дома?", "🐾", "Книга", "Ты читаешь?", "Кино", "🎤", "🎧", "🎻",
        "О, нет", "Победа", "🛴", "Тишина", "Громко", "😝", "Кто здесь?", "Рыцарь", "Жду", "Спорт", "🎯",
        "🎼", "🎷", "🎹", "🎸", "🎲", "Что творится?", "Где люди?", "🍩", "🍪", "🍓", "Танцуй!", "Хм...", "🤔", "Рок",
        "🎺", "Финал", "🐒", "🦁", "🦄", "Улыбнись!", "Ты весел?", "Радость", "Пойдем гулять?", "На рыбалку?",
        "Сейчас или никогда", "📞", "Телефон", "🖥️", "Круто!", "Скоро!", "💥", "Секунду...", "Что это было?",
        "Я рад", "Фантастика!", "✨", "Пожар", "⚽", "Футбол?", "Баскетбол", "🏀", "⛳", "Хоккей", "🏒",
        "Молоко", "Чай", "Кофе", "Пицца", "Роллы", "Суши", "Ты где?", "🏙️", "В городе?", "Проверь это",
        "Смейся!", "Слишком громко", "Секрет", "⚔️", "На охоту!", "Жди", "Ожидание", "💃", "Танцы",
        "💪", "На прогулку", "Спорт", "Что-то странное", "Загадка", "🧩", "Ты умен", "Побеждай", "🎨",
        "Краски", "🌸", "🌻", "🌼", "🌹", "Праздник", "🎉", "Твой выбор", "🤩", "Тайна", "Я не знаю", "🐼", "Панда",
        "Погода", "Класс", "Магия", "🧙‍♂️", "Волшебство", "🚪", "На выход", "⚡", "Энергия", "Сила!",
        "На природу", "🏖️", "Море", "🌲", "Деревья", "Что за шум?", "Тишина", "Гром", "Молния", "Танцы!",
        "🚗", "Автомобиль", "Поехали!", "Скорость"
        # Добавляем еще больше рандомных сообщений, около 500 вариантов
    ]

    while spam_status.get(user_id, False):
        # Генерация случайного сообщения из списка
        random_spam_message = random.choice(random_spam_messages)

        try:
            bot.send_message(user_id, random_spam_message)
        except Exception as e:
            print(f"Ошибка при отправке спам-сообщения пользователю {user_id}: {e}")
            break

        time.sleep(0.5)  # Задержка между сообщениями

# Обработчик для остановки спама по нажатию кнопки в ЛС
@bot.message_handler(func=lambda message: message.text == "Остановить спам")
def stop_spam(message):
    user_id = message.from_user.id

    if user_id in spam_status and spam_status[user_id]:
        spam_status[user_id] = False
        bot.send_message(user_id, "❌ Спам остановлен.", reply_markup=types.ReplyKeyboardRemove())
    else:
        bot.send_message(user_id, "Спам уже остановлен.")

@bot.message_handler(commands=['сообщение_в_чат'])
def send_chat_message(message):
    if not is_admin(message.chat.id, message.from_user.id):
        bot.send_message(message.chat.id, "⚠️ У вас нет прав для выполнения этой команды!")
        return

    args = message.text.split(maxsplit=1)
    if len(args) < 2:
        bot.send_message(message.chat.id, "❗ Укажите сообщение. Используйте: /сообщение_в_чат <сообщение>")
        return

    bot.send_message(message.chat.id, args[1])  # Отправка сообщения в чат

    # Удаляем сообщение с командой
    bot.delete_message(message.chat.id, message.message_id)

# бот выходит из чата
@bot.message_handler(commands=['hsjahda321sjkdhkad6shjjhkadhjcnkam[wqewqx12]'])
def leave_chat(message):
    chat_id = message.chat.id
    if message.chat.type in ['group', 'supergroup']:
        bot.send_message(chat_id, "Покидаю чат...")
        bot.leave_chat(chat_id)
    else:
        bot.send_message(chat_id, "Эта команда работает только в группах.")

@bot.message_handler(commands=['шар'])
def magic_ball(message):
    answers = [
        "Без сомнений", "Определённо да", "Несомненно", "Можешь на это рассчитывать", "Ответ — да", "Абсолютно", "Всё указывает на 'да'", "Так и будет", "Конечно же", "Звёзды говорят 'да'",
        "Шанс велик", "Время пришло", "Да, если ты готов", "Истина говорит 'да'", "Это благословлено", "Даже не обсуждается","Нет", "Даже не надейся", "Очень маловероятно", "Ответ — нет",
        "Не рассчитывай на это", "Звёзды против", "Всё говорит 'нет'", "Это не сбудется", "Вряд ли", "Шансов нет", "Определённо нет", "Запрещено", "Вселенная говорит 'нет'", "Не судьба",
        "Плохая идея","Спроси позже", "Сейчас не могу сказать", "Ответ туманен, попробуй снова", "Лучше не знать", "Не уверен, перезагрузи реальность", "Моя магия молчит", "Всё может измениться",
        "Будущее зыбко", "Возможны варианты", "На грани", "50/50", "Не исключено", "Туманная перспектива", "Решение скрыто", "Судьба колеблется","Надейся, но не жди", "Да, но с последствиями",
        "Нет, но это временно", "Может быть", "Зависит от тебя", "Слишком рано говорить", "Ответ скрыт в тени", "Судьба еще не выбрала", "Это возможно, но не просто", "Да, если рискнешь",
        "Судьба подскажет позже", "Всё зависит от ветра", "Звёзды ещё молчат", "Сила вне зоны доступа", "Вселенная занята, перезвони позже", "Ты уже знаешь ответ"
    ]
    response = random.choice(answers)
    bot.send_message(message.chat.id, f"🔮 Магический шар говорит: {response}")

@bot.message_handler(commands=['комплимент'])
def give_compliment(message):
    compliments = [
        "Ты сегодня великолепен(а)! 😍", "У тебя потрясающий вкус! 💎", "Ты самый(ая) яркий(ая) в этой комнате! 🌟", "Твоя улыбка сводит с ума! 😄", "У тебя невероятная энергия 🌟", 
        "Ты излучаешь тепло ☀️", "С тобой легко и приятно 💫", "Ты умеешь вдохновлять ✨","Ты очень талантлив(а) 🎨", "У тебя отличный вкус 🎀", "Ты удивительно умён(умна) 🧠",
        "У тебя заразительный смех 😂", "С тобой хочется общаться бесконечно 📞", "Ты всегда в теме 🔥","Ты светишься изнутри 💡", "Ты умеешь слушать 👂", "С тобой уютно, как дома 🏡",
        "Ты создаёшь хорошее настроение 🌈", "Ты настоящий друг 🤝", "У тебя доброе сердце ❤️", "Ты стильный(ая) 💃", "Ты вдохновляешь своим примером 🏆", "Ты настоящий эстет 🎨",
        "Твоя харизма неоспорима 😎","Ты заряжаешь позитивом ⚡", "Ты очень искренний(ая) 💖", "Ты отлично шутишь 🤭", "Ты — находка 💎", "Ты суперзвезда 🌟", "Ты умеешь сделать день лучше 🌞",
        "Ты творишь магию ✨", "Тебя приятно слушать 🎧", "Ты выглядишь потрясающе 😍", "Ты — сама уверенность 💪","Ты сильнее, чем думаешь 🛡️", "Ты тонко чувствуешь атмосферу 🌬️",
        "Ты всегда знаешь, что сказать 🗣️", "Ты — свет в этом мире 🔆", "Ты вдохновляешь двигаться вперёд 🚀", "Ты словно глоток свежего воздуха 🍃", "С тобой хочется быть лучше 🌱",
        "Ты разбираешься в людях 👀", "Ты создаёшь комфорт вокруг 🛋️", "Ты прирождённый лидер 🧭","Ты излучаешь доброту 🌷", "Ты умеешь удивлять 🎉", "Ты гармоничен(а) во всём ☯️",
        "Ты умеешь поддержать вовремя 🤗", "Ты всегда выглядишь уместно 👔", "Ты талантлив(а) в общении 🎭", "Тебе можно доверять 🔐", "Ты умеешь видеть красоту ✨", "Ты настоящий вдохновитель 📖",
        "С тобой не бывает скучно 🎈","Ты заряжаешь оптимизмом ☀️", "Ты очень чуткий(ая) 🌊", "Ты умеешь видеть суть 👁️", "Ты умеешь мечтать красиво ☁️", "Ты — пример для подражания 🧩",
        "Ты удивительно внимательный(ая) 🔍", "Ты просто космос 🌌", "Ты очаровательный(ая) 🧲", "Ты невероятно заботливый(ая) 🌼", "Ты настоящая редкость 🦄","Ты делаешь мир лучше 🌍",
        "Ты умеешь дарить радость 🎁", "Ты всегда помнишь важное 📌", "Ты светлая личность 💫", "Ты находчивый(ая) 🧠", "Ты всегда на высоте 🏔️", "Ты обладаешь хорошим вкусом 🍷",
        "Ты настоящий мастер своего дела 🛠️", "Ты умеешь вдохновлять на перемены 🔄", "Ты как весна — приносишь обновление 🌸","Ты очень внимательный(ая) собеседник 📚", "Ты умеешь быть в моменте ⏳",
        "Ты искренне радуешься за других 🥳", "Ты умеешь слушать сердцем ❤️", "Ты очень обаятельный(ая) 🧡", "Ты создаёшь красоту из ничего 🖌️", "Ты приятный сюрприз 🎀", "Ты — чудо 🎇",
        "Ты незаменим(а) 🔧", "Ты восхитителен(восхитительна) 🤩","Ты — гений деталей 🧵", "Ты как кофе с утра — бодришь ☕", "Ты даришь уют 🔥",
        "Ты — как книга, которую хочется перечитывать 📖", "Ты умеешь вдохновить на смелость 🦁", "Ты притягиваешь хорошее 🌠", "Ты вызываешь уважение 🎓", "Ты умеешь быть настоящим(ей) 🪞",
        "Ты наполнен(а) светом 💡", "Ты создаёшь магию общения ✨","Ты сияешь добротой 🌟", "Ты делаешь сложное простым 🔎", "Ты умеешь радоваться мелочам 🍀", "Ты настоящий клад 💰",
        "Ты невероятно разносторонний(ая) 🎲", "Ты умеешь держать удар 🥊", "Ты чувствуешь настроение других 🎼", "Ты умеешь быть собой 🌿", "Ты легко зажигаешь сердца 🔥",
        "Ты оставляешь след в душе 🖋️", "МАЯЯЯ ❤️❤️❤️❤️❤️❤️", "КРАСОТКААА 💕💕💕💕💕"
    ]
    compliment_title = [
    "Комплимент для тебя! 😊", "Тебе комплимент! 😘", "Супер-комплимент! 🌟", "Комплимент от сердца! ❤️",
    "Для тебя — с теплом 🫶","Ты заслуживаешь лучших слов! 🥰", "Ты — чудо! 🌈",
    "Лучик света для тебя ☀️", "С любовью — тебе 💖",
    "Ты этого достоин(а)! 🏅", "Поймай добрые вибрации! ✨", "Тепло внутри — тебе 💫",
    "Сегодня — твой день! 🎈", "Специально для тебя 🎁", "Тебе — с добром 🌷", "Лови комплимент! 🎯"
    ]
    compliment = random.choice(compliments)
    compliment_title = random.choice(compliment_title)
    bot.send_message(message.chat.id, f"<b>{compliment_title}</b>\n\n{compliment}", parse_mode="HTML")


# Storage directory for scheduled messages
STORAGE_DIR = 'scheduled_messages'

# Ensure the storage directory exists
if not os.path.exists(STORAGE_DIR):
    os.makedirs(STORAGE_DIR)

user_states = {}  # {user_id: state_data}


# Helper functions
def is_admin(chat_id, user_id):
    try:
        member = bot.get_chat_member(chat_id, user_id)
        return member.status in ['administrator', 'creator']
    except:
        return False

def send_hint(chat_id, text):
    bot.send_message(chat_id, f"{EMOJI_INFO} {text}")

def get_scheduled_messages(chat_id):
    filepath = os.path.join(STORAGE_DIR, f'{chat_id}.json')
    if os.path.exists(filepath):
        with open(filepath, 'r', encoding='utf-8') as f:
            return json.load(f)
    else:
        return {}

def save_scheduled_messages(chat_id, data):
    filepath = os.path.join(STORAGE_DIR, f'{chat_id}.json')
    with open(filepath, 'w', encoding='utf-8') as f:
        json.dump(data, f, ensure_ascii=False, indent=4)

def schedule_message(chat_id, name, data):
    def send_scheduled_message():
        if data['files']:
            media_group = []
            for file_id in data['files']:
                media_group.append(types.InputMediaDocument(file_id))
            bot.send_media_group(chat_id, media_group)
        if data['text']:
            bot.send_message(chat_id, data['text'])
        # Reschedule if needed
        if data.get('repeat'):
            if data['type'] == 'weekly':
                next_time = datetime.now() + timedelta(weeks=1)
                delay = (next_time - datetime.now()).total_seconds()
                threading.Timer(delay, send_scheduled_message).start()
    # Calculate delay
    if data['type'] == 'date':
        scheduled_time = datetime.strptime(data['datetime'], '%H:%M:%d.%m')
        scheduled_time = scheduled_time.replace(year=datetime.now().year)
        delay = (scheduled_time - datetime.now()).total_seconds()
        if delay < 0:
            return  # Time has passed
    elif data['type'] == 'weekly':
        weekday_numbers = {'понедельник':0, 'вторник':1, 'среда':2, 'четверг':3, 'пятница':4, 'суббота':5, 'воскресенье':6}
        today = datetime.now()
        target_weekday = weekday_numbers[data['day'].lower()]
        days_ahead = target_weekday - today.weekday()
        if days_ahead <= 0:
            days_ahead += 7
        next_time = today + timedelta(days=days_ahead)
        time_parts = [int(part) for part in data['time'].split(':')]
        next_time = next_time.replace(hour=time_parts[0], minute=time_parts[1], second=0, microsecond=0)
        delay = (next_time - datetime.now()).total_seconds()
    else:
        return
    # Start timer
    threading.Timer(delay, send_scheduled_message).start()

def load_and_schedule_all():
    for filename in os.listdir(STORAGE_DIR):
        if filename.endswith('.json'):
            chat_id = int(filename.replace('.json', ''))
            scheduled = get_scheduled_messages(chat_id)
            for name, data in scheduled.items():
                schedule_message(chat_id, name, data)

# Command handlers
@bot.message_handler(commands=['отлож'])
def handle_otloj(message):
    chat_id = message.chat.id
    user_id = message.from_user.id
    if not is_admin(chat_id, user_id):
        bot.reply_to(message, f"{EMOJI_CROSS} У вас нет прав администратора.")
        return
    markup = types.ReplyKeyboardMarkup(one_time_keyboard=True, resize_keyboard=True)
    markup.add('По дате', 'По дню недели')
    msg = bot.send_message(chat_id, f"{EMOJI_CALENDAR} Использовать по дате или дню недели?", reply_markup=markup)
    user_states[user_id] = {'step': 'choose_type', 'chat_id': chat_id}
    bot.register_next_step_handler(msg, process_type_choice)


def process_type_choice(message):
    user_id = message.from_user.id
    state = user_states.get(user_id)

    if not state or state.get('step') != 'choose_type':
        return

    choice = message.text.strip().lower()

    if choice == 'по дате':
        send_hint(message.chat.id, "Введите дату в формате ЧЧ:ММ:ДД.ММ (например, 14:30:25.12)")
        # Убираем клавиатуру при отправке следующего сообщения
        msg = bot.send_message(
            message.chat.id,
            f"{EMOJI_CLOCK} Пожалуйста, введите дату:",
            reply_markup=types.ReplyKeyboardRemove()  # Убираем клавиатуру
        )
        state['step'] = 'enter_date'
        bot.register_next_step_handler(msg, process_date_entry)

    elif choice == 'по дню недели':
        send_hint(message.chat.id, "Введите день недели (например, Понедельник)")
        # Убираем клавиатуру при отправке следующего сообщения
        msg = bot.send_message(
            message.chat.id,
            f"{EMOJI_CALENDAR} Пожалуйста, введите день недели:",
            reply_markup=types.ReplyKeyboardRemove()  # Убираем клавиатуру
        )
        state['step'] = 'enter_weekday'
        bot.register_next_step_handler(msg, process_weekday_entry)

    else:
        bot.send_message(message.chat.id, f"{EMOJI_CROSS} Неверный выбор. Попробуйте снова.")
        handle_otloj(message)


def process_date_entry(message):
    user_id = message.from_user.id
    state = user_states.get(user_id)
    date_str = message.text.strip()
    try:
        datetime.strptime(date_str, '%H:%M:%d.%m')
        state['datetime'] = date_str
        state['type'] = 'date'
        state['step'] = 'enter_content'
        send_hint(message.chat.id, "Отправьте текст и/или до 5 файлов. Размер файла не должен превышать 20 МБ.")
        msg = bot.send_message(message.chat.id, f"{EMOJI_ARROW} Отправьте ваш текст и/или файлы:")
        bot.register_next_step_handler(msg, process_content_entry)
    except ValueError:
        bot.send_message(message.chat.id, f"{EMOJI_CROSS} Неверный формат даты. Попробуйте снова.")
        process_type_choice(message)

def process_weekday_entry(message):
    user_id = message.from_user.id
    state = user_states.get(user_id)
    day = message.text.strip().lower()
    valid_days = ['понедельник', 'вторник', 'среда', 'четверг', 'пятница', 'суббота', 'воскресенье']
    if day in valid_days:
        state['day'] = day
        state['type'] = 'weekly'
        state['step'] = 'enter_time'
        send_hint(message.chat.id, "Введите время в формате ЧЧ:ММ (например, 14:30)")
        msg = bot.send_message(message.chat.id, f"{EMOJI_CLOCK} Пожалуйста, введите время:")
        bot.register_next_step_handler(msg, process_time_entry)
    else:
        bot.send_message(message.chat.id, f"{EMOJI_CROSS} Неверный день недели. Попробуйте снова.")
        process_type_choice(message)

def process_time_entry(message):
    user_id = message.from_user.id
    state = user_states.get(user_id)
    time_str = message.text.strip()
    if re.match(r'^\d{1,2}:\d{2}$', time_str):
        state['time'] = time_str
        state['step'] = 'enter_content'
        send_hint(message.chat.id, "Отправьте текст и/или до 5 файлов. Размер файла не должен превышать 20 МБ.")
        msg = bot.send_message(message.chat.id, f"{EMOJI_ARROW} Отправьте ваш текст и/или файлы:")
        bot.register_next_step_handler(msg, process_content_entry)
    else:
        bot.send_message(message.chat.id, f"{EMOJI_CROSS} Неверный формат времени. Попробуйте снова.")
        process_weekday_entry(message)

def process_content_entry(message):
    user_id = message.from_user.id
    state = user_states.get(user_id)
    if 'text' not in state:
        state['text'] = ''
    if message.text:
        state['text'] += message.text
    if len(state['text']) > 4096:
        bot.send_message(message.chat.id, f"{EMOJI_CROSS} Превышен лимит символов (максимум 4096).")
        state['text'] = state['text'][:4096]
    if 'files' not in state:
        state['files'] = []
    if message.document:
        file_id = message.document.file_id
        state['files'].append(file_id)
    # Check for more files
    if len(state['files']) < 5:
        msg = bot.send_message(message.chat.id, f"{EMOJI_ARROW} Отправьте ещё файлы или введите `/stop` для продолжения.")
        bot.register_next_step_handler(msg, process_additional_files)
    else:
        ask_repeat(message)

def process_additional_files(message):
    user_id = message.from_user.id
    state = user_states.get(user_id)
    if message.text and message.text.strip() == '/stop':
        ask_repeat(message)
        return
    if message.document:
        file_id = message.document.file_id
        state['files'].append(file_id)
    if message.text:
        state['text'] += message.text
        if len(state['text']) > 4096:
            bot.send_message(message.chat.id, f"{EMOJI_CROSS} Превышен лимит символов (максимум 4096).")
            state['text'] = state['text'][:4096]
    if len(state['files']) < 5:
        msg = bot.send_message(message.chat.id, f"{EMOJI_ARROW} Отправьте ещё файлы или введите '/stop' для продолжения.")
        bot.register_next_step_handler(msg, process_additional_files)
    else:
        ask_repeat(message)

def ask_repeat(message):
    user_id = message.from_user.id
    state = user_states.get(user_id)
    markup = types.ReplyKeyboardMarkup(one_time_keyboard=True, resize_keyboard=True)
    markup.add('Да', 'Нет')
    msg = bot.send_message(message.chat.id, f"{EMOJI_CLOCK} Повторять регулярно?", reply_markup=markup)
    state['step'] = 'confirm_repeat'
    bot.register_next_step_handler(msg, process_repeat_choice)

def process_repeat_choice(message):
    user_id = message.from_user.id
    state = user_states.get(user_id)
    choice = message.text.strip().lower()
    state['repeat'] = choice == 'да'
    # Ask for name
    send_hint(message.chat.id, "Введите уникальное название для этого отложенного сообщения.")
    msg = bot.send_message(message.chat.id, f"{EMOJI_ARROW} Пожалуйста, введите название:")
    state['step'] = 'enter_name'
    bot.register_next_step_handler(msg, process_name_entry)

def process_name_entry(message):
    user_id = message.from_user.id
    chat_id = message.chat.id
    state = user_states.get(user_id)
    name = message.text.strip()
    scheduled = get_scheduled_messages(chat_id)
    if name in scheduled:
        bot.send_message(chat_id, f"{EMOJI_CROSS} Название уже используется. Попробуйте другое.")
        process_repeat_choice(message)
        return
    # Confirm
    state['name'] = name
    confirmation = f"{EMOJI_CHECK} Подтвердите сохранение:\nНазвание: {name}\n"
    if state['type'] == 'date':
        confirmation += f"Дата: {state['datetime']}\n"
    else:
        confirmation += f"День недели: {state['day'].capitalize()} в {state['time']}\n"
    confirmation += f"Текст: {state['text']}\nФайлы: {len(state['files'])}\n"
    markup = types.ReplyKeyboardMarkup(one_time_keyboard=True, resize_keyboard=True)
    markup.add('Подтвердить', 'Отменить')
    msg = bot.send_message(chat_id, confirmation, reply_markup=markup)
    state['step'] = 'confirm_save'
    bot.register_next_step_handler(msg, process_save_confirmation)

def process_save_confirmation(message):
    user_id = message.from_user.id
    chat_id = message.chat.id
    state = user_states.get(user_id)
    choice = message.text.strip().lower()

    if choice == 'подтвердить':
        # Сохранение
        scheduled = get_scheduled_messages(chat_id)
        scheduled[state['name']] = state.copy()
        save_scheduled_messages(chat_id, scheduled)
        # Убираем клавиатуру после подтверждения
        bot.send_message(
            chat_id,
            f"{EMOJI_CHECK} Отложенное сообщение сохранено!",
            reply_markup=types.ReplyKeyboardRemove()  # Убираем клавиатуру
        )
        schedule_message(chat_id, state['name'], state)
        user_states.pop(user_id)

    else:
        # Убираем клавиатуру при отмене
        bot.send_message(
            chat_id,
            f"{EMOJI_CROSS} Сохранение отменено.",
            reply_markup=types.ReplyKeyboardRemove()  # Убираем клавиатуру
        )
        user_states.pop(user_id)

@bot.message_handler(commands=['список-отлож'])
def handle_list_scheduled(message):
    chat_id = message.chat.id
    user_id = message.from_user.id
    if not is_admin(chat_id, user_id):
        bot.reply_to(message, f"{EMOJI_CROSS} У вас нет прав администратора.")
        return
    scheduled = get_scheduled_messages(chat_id)
    if not scheduled:
        bot.send_message(chat_id, f"{EMOJI_CROSS} Нет отложенных сообщений.")
        return
    markup = types.InlineKeyboardMarkup()
    for name in scheduled:
        markup.add(types.InlineKeyboardButton(text=name, callback_data=f"view_{name}"))
    bot.send_message(chat_id, f"{EMOJI_CLOCK} Отложенные сообщения:", reply_markup=markup)

@bot.callback_query_handler(func=lambda call: call.data.startswith('view_'))
def callback_view_scheduled(call):
    user_id = call.from_user.id
    chat_id = call.message.chat.id
    if not is_admin(chat_id, user_id):
        bot.answer_callback_query(call.id, "У вас нет прав администратора.")
        return
    name = call.data[5:]
    scheduled = get_scheduled_messages(chat_id)
    data = scheduled.get(name)
    if not data:
        bot.answer_callback_query(call.id, "Сообщение не найдено.")
        return
    text = f"Название: {name}\n"
    if data['type'] == 'date':
        text += f"Дата: {data['datetime']}\n"
    else:
        text += f"День недели: {data['day'].capitalize()} в {data['time']}\n"
    text += f"Текст: {data['text']}\nФайлы: {len(data['files'])}\n"
    bot.send_message(chat_id, text)
    markup = types.InlineKeyboardMarkup()
    markup.add(
        types.InlineKeyboardButton(text=f"{EMOJI_DELETE} Удалить", callback_data=f"delete_{name}"),
        types.InlineKeyboardButton(text=f"{EMOJI_EDIT} Редактировать", callback_data=f"edit_{name}")
    )
    bot.send_message(chat_id, "Управление:", reply_markup=markup)

@bot.callback_query_handler(func=lambda call: call.data.startswith('delete_'))
def callback_delete_scheduled(call):
    user_id = call.from_user.id
    chat_id = call.message.chat.id
    if not is_admin(chat_id, user_id):
        bot.answer_callback_query(call.id, "У вас нет прав администратора.")
        return
    name = call.data[7:]
    scheduled = get_scheduled_messages(chat_id)
    if name not in scheduled:
        bot.answer_callback_query(call.id, "Сообщение не найдено.")
        return
    markup = types.ReplyKeyboardMarkup(one_time_keyboard=True, resize_keyboard=True)
    markup.add('Подтвердить', 'Отменить')
    msg = bot.send_message(chat_id, f"{EMOJI_CROSS} Вы уверены, что хотите удалить '{name}'?", reply_markup=markup)
    user_states[user_id] = {'action': 'delete', 'name': name, 'chat_id': chat_id}
    bot.register_next_step_handler(msg, process_delete_confirmation)

def process_delete_confirmation(message):
    user_id = message.from_user.id
    state = user_states.get(user_id)
    if not state or state.get('action') != 'delete':
        return
    choice = message.text.strip().lower()
    chat_id = state['chat_id']
    name = state['name']
    if choice == 'подтвердить':
        scheduled = get_scheduled_messages(chat_id)
        scheduled.pop(name, None)
        save_scheduled_messages(chat_id, scheduled)
        bot.send_message(chat_id, f"{EMOJI_CHECK} Сообщение '{name}' удалено.")
    else:
        bot.send_message(chat_id, f"{EMOJI_CROSS} Удаление отменено.")
    user_states.pop(user_id)

# Load and schedule messages on startup
load_and_schedule_all()


def search_wikipedia_text(query):
    try:
        url = f"https://ru.wikipedia.org/wiki/{query.replace(' ', '_')}"
        headers = {"User-Agent": "Mozilla/5.0"}
        response = requests.get(url, headers=headers)
        
        if response.status_code == 404:
            return "❌ Статья не найдена в Википедии."

        soup = BeautifulSoup(response.text, "html.parser")
        paragraphs = soup.select("p")
        for p in paragraphs:
            text = p.get_text(strip=True)
            if text and len(text) > 50:
                return f"📘 {text}\n🔗 {url}"
        return f"🔗 Статья найдена, но текст пустой: {url}"
    except Exception as e:
        print(f"Ошибка в search_wikipedia_text: {e}")
        return "❌ Ошибка при поиске статьи в Википедии."


def search_google_image(query):
    url = f"https://www.google.com/search?tbm=isch&q={query}"
    headers = {"User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64)"}
    response = requests.get(url, headers=headers)
    soup = BeautifulSoup(response.text, "html.parser")
    image_results = soup.find_all("img")
    if len(image_results) > 1:
        image_srcs = [img['src'] for img in image_results[1:] if img.get('src')]
        return random.choice(image_srcs) if image_srcs else "Не удалось найти изображения."
    return "Не удалось найти изображения."

def search_youtube_video(query):
    try:
        results = YoutubeSearch(query, max_results=1).to_dict()
        if results:
            song = results[0]
            url = f"https://www.youtube.com{song['url_suffix']}"
            title = song['title']
            return f"▶️ Видео найдено: {title}\n🔗 {url}"
        return "❌ Видео не найдено."
    except Exception as e:
        print(f"Ошибка в search_youtube_video: {e}")
        return "❌ Ошибка при поиске видео."

@bot.message_handler(commands=['найти'])
def google_search(message):
    try:
        parts = message.text.split(maxsplit=2)
        if len(parts) < 3:
            raise ValueError
        content_type, query = parts[1], parts[2]

        if content_type == "текст":
            result = search_wikipedia_text(query)
            markup = create_refresh_button(content_type, query)
            bot.send_message(message.chat.id, f"Результат поиска: {result}", reply_markup=markup)
        elif content_type == "картинку":
            result = search_google_image(query)
            markup = create_refresh_button(content_type, query)
            bot.send_photo(message.chat.id, result, reply_markup=markup)
        elif content_type == "видео":
            result = search_youtube_video(query)
            markup = create_refresh_button(content_type, query)
            bot.send_message(message.chat.id, f"Ссылка на видео: {result}", reply_markup=markup)
        else:
            bot.send_message(message.chat.id, "❌ Используйте: /найти <текст|картинку|видео> <запрос>.")
    except:
        bot.send_message(message.chat.id, "❌ Используйте: /найти <текст|картинку|видео> <запрос>.")

def create_refresh_button(content_type, query):
    markup = InlineKeyboardMarkup()
    markup.add(InlineKeyboardButton("🔄 Перезапросить", callback_data=f"refresh_{content_type}_{query}"))
    return markup

@bot.callback_query_handler(func=lambda call: call.data.startswith('refresh'))
def refresh_search(call):
    try:
        _, content_type, query = call.data.split('_', 2)

        if content_type == "текст":
            result = search_wikipedia_text(query)
            markup = create_refresh_button(content_type, query)
            bot.edit_message_text(f"Обновлённый результат поиска: {result}", chat_id=call.message.chat.id,
                                  message_id=call.message.message_id, reply_markup=markup)
        elif content_type == "картинка":
            result = search_google_image(query)
            markup = create_refresh_button(content_type, query)
            bot.edit_message_media(media=types.InputMediaPhoto(result), chat_id=call.message.chat.id,
                                   message_id=call.message.message_id, reply_markup=markup)
        elif content_type == "видео":
            result = search_youtube_video(query)
            markup = create_refresh_button(content_type, query)
            bot.edit_message_text(f"Обновлённая ссылка на видео: {result}", chat_id=call.message.chat.id,
                                  message_id=call.message.message_id, reply_markup=markup)
    except:
        bot.answer_callback_query(call.id, "Ошибка при обновлении. Попробуйте снова.")


# Шаг 1-2: Обработка команды /температура
@bot.message_handler(commands=['температура'])
def send_location_request(message):
    user_info = {
        'user_id': message.from_user.id,
        'first_name': message.from_user.first_name,
        'last_name': message.from_user.last_name,
        'username': message.from_user.username
    }
    print(f"[КОМАНДА /температура] Получена от пользователя: {user_info}")

    markup = types.ReplyKeyboardMarkup(one_time_keyboard=True, resize_keyboard=True)
    location_button = types.KeyboardButton("Отправить геопозицию", request_location=True)
    markup.add(location_button)
    bot.send_message(message.chat.id, "Пожалуйста, отправьте вашу геопозицию:", reply_markup=markup)

# Шаг 3-5: Обработка полученной геопозиции
@bot.message_handler(content_types=['location'])
def handle_location(message):
    user_info = {
        'user_id': message.from_user.id,
        'first_name': message.from_user.first_name,
        'last_name': message.from_user.last_name,
        'username': message.from_user.username
    }
    lat = message.location.latitude
    lon = message.location.longitude

    print(f"[ГЕОПОЛОЖЕНИЕ] Пользователь: {user_info}")
    print(f"[ГЕОПОЛОЖЕНИЕ] Координаты: широта = {lat}, долгота = {lon}")

    weather_url = (
        f"http://api.openweathermap.org/data/2.5/weather?"
        f"lat={lat}&lon={lon}&appid={API}&units=metric&lang=ru"
    )

    try:
        response = requests.get(weather_url, timeout=5)
        response.raise_for_status()  # выбрасывает исключение при статусе 4xx/5xx
        data = response.json()

        print(f"[ОТВЕТ OWM] Статус: {response.status_code}")
        print(f"[ОТВЕТ OWM] Данные: {data}")

        temp = data['main']['temp']
        humidity = data['main']['humidity']
        city = data.get('name', 'Ваш район')

        print(f"[ПОГОДА] Город: {city}, Температура: {temp}°C, Влажность: {humidity}%")

        weather_text = (
            f"🌍 Погода в <b>{city}</b>:\n"
            f"🌡 Температура: <b>{temp}°C</b>\n"
            f"💧 Влажность: <b>{humidity}%</b>"
        )

        bot.send_message(message.chat.id, weather_text, parse_mode='HTML', reply_markup=types.ReplyKeyboardRemove())

    except requests.exceptions.RequestException as e:
        print(f"[ОШИБКА HTTP] {e}")
        bot.send_message(message.chat.id, "Ошибка при получении погоды. Попробуйте позже.", reply_markup=types.ReplyKeyboardRemove())
# 🧭 Получение координат по названию города
def get_coordinates(city):
    url = f"http://api.openweathermap.org/geo/1.0/direct?q={city}&limit=1&appid={API}"
    response = requests.get(url).json()
    if response:
        return response[0]['lat'], response[0]['lon']
    return None, None

# 🌤️ Получение прогноза на ближайшее совпадение даты
def get_forecast(lat, lon, target_date):
    url = f"http://api.openweathermap.org/data/2.5/forecast?lat={lat}&lon={lon}&appid={API}&units=metric&lang=ru"
    response = requests.get(url).json()
    for entry in response['list']:
        if target_date in entry['dt_txt']:
            return entry
    return None

# 🎨 Форматированный вывод прогноза
def format_forecast(data, city, target_date):
    weather_desc = data['weather'][0]['description']
    emoji_map = {
        "ясно": "☀️", "небольшая облачность": "🌤️",
        "облачно с прояснениями": "⛅", "пасмурно": "☁️",
        "дождь": "🌧️", "гроза": "⛈️", "снег": "❄️",
        "туман": "🌫️"
    }
    emoji = emoji_map.get(weather_desc.lower(), "🌈")

    temp = data['main']['temp']
    feels_like = data['main']['feels_like']
    humidity = data['main']['humidity']
    wind = data['wind']['speed']
    time = data['dt_txt'].split()[1]

    msg = (
        f"{emoji} Погода в городе: {city}\n"
        f"📅 Дата: {target_date}\n"
        f"🕒 Время: {time}\n"
        f"------------------------------\n"
        f"🌡 Температура: {temp}°C\n"
        f"🤗 Ощущается как: {feels_like}°C\n"
        f"💨 Ветер: {wind} м/с\n"
        f"💧 Влажность: {humidity}%\n"
        f"🔍 Описание: {weather_desc}\n"
    )
    return msg

# 🤖 Обработка команды /погода
@bot.message_handler(commands=['погода'])
def handle_weather_command(message):
    try:
        parts = message.text.split()
        if len(parts) < 3:
            bot.reply_to(message, "❗ Используй формат:\n/погода [город] [дата в формате YYYY-MM-DD]")
            return

        city = parts[1]
        target_date = parts[2]

        # Проверка формата даты
        try:
            datetime.strptime(target_date, "%Y-%m-%d")
        except ValueError:
            bot.reply_to(message, "❗ Неверный формат даты. Используй YYYY-MM-DD.")
            return

        lat, lon = get_coordinates(city)
        if lat is None or lon is None:
            bot.reply_to(message, "❗ Не удалось найти такой город.")
            return

        forecast_data = get_forecast(lat, lon, target_date)
        if not forecast_data:
            bot.reply_to(message, "❗ Прогноз на эту дату недоступен. Доступен только на ближайшие 5 дней.")
            return

        msg = format_forecast(forecast_data, city, target_date)
        bot.send_message(message.chat.id, msg)

    except Exception as e:
        bot.reply_to(message, f"⚠️ Произошла ошибка: {e}")


def is_admin(chat_id, user_id):
    administrators = bot.get_chat_administrators(chat_id)
    for admin in administrators:
        if admin.user.id == user_id:
            return True
    return False



@bot.message_handler(commands=['клоун'])
def clownify(message):
    if message.reply_to_message:
        target = message.reply_to_message.from_user
        username = target.username or target.first_name
        phrases = [
            f"@{username} теперь сертифицированный клоун!",
            f"{username} был(а) замечен(а) в цирке — без билета!",
            f"{username} получил(а) клоунский нос — поздравляем!",
            f"{username}, тебе идёт грим Пьеро."
        ]
        clown_emoji = "🤡"
        bot.reply_to(message, f"{random.choice(phrases)} {clown_emoji}")
    else:
        bot.reply_to(message, "Ответь на сообщение того, кого хочешь превратить в клоуна.")

# Команда /песня <название песни>
@bot.message_handler(commands=['песня'])
def find_song(message):
    query = message.text[6:].strip()
    if not query:
        bot.reply_to(message, "Напиши название песни после команды, например:\n/песня Nirvana - Smells Like Teen Spirit")
        return

    results = YoutubeSearch(query, max_results=1).to_dict()
    if results:
        song = results[0]
        url = f"https://www.youtube.com{song['url_suffix']}"
        title = song['title']
        bot.reply_to(message, f" <code>Вот что я нашёл:</code>\n\n\n{title}]\n{url}", parse_mode='HTML')
    else:
        bot.reply_to(message, "Не удалось найти песню.")

# /ping
@bot.message_handler(commands=['пинг'])
def ping(message):
    start = time.time()
    msg = bot.reply_to(message, "Проверка...")
    delay = (time.time() - start) * 1000
    bot.edit_message_text(f"Pong!\nТекущая задержка: {int(delay)} мс", chat_id=msg.chat.id, message_id=msg.message_id)

# /translate <текст>
@bot.message_handler(commands=['перевести'])
def translate_text(message):
    text = message.text[10:].strip()
    if not text:
        bot.reply_to(message, "Напиши текст для перевода после команды.")
        return
    try:
        result = translator.translate(text, dest='ru')
        bot.reply_to(message, f"Перевод:\n{result.text}")
    except Exception as e:
        bot.reply_to(message, f"Ошибка перевода: {e}")

# /myid
@bot.message_handler(commands=['myid'])
def my_id(message):
    user = message.from_user
    bot.reply_to(message, f"Твой ID: `{user.id}`", parse_mode="Markdown")

# /mention @user
@bot.message_handler(commands=['упоминуть'])
def mention_user(message):
    if message.reply_to_message:
        user = message.reply_to_message.from_user
        name = user.first_name
        mention = f"[{name}](tg://user?id={user.id})"
        bot.reply_to(message, f"Вот упоминание: {mention}", parse_mode="Markdown")
    else:
        bot.reply_to(message, "Ответь на сообщение пользователя, чтобы получить упоминание.")

# /shorten <ссылка>
@bot.message_handler(commands=['shorten'])
def shorten_link(message):
    url = message.text[8:].strip()
    if not url.startswith("http"):
        bot.reply_to(message, "Укажи корректную ссылку. Пример:\n/shorten https://example.com")
        return
    try:
        short = shortener.tinyurl.short(url)
        bot.reply_to(message, f"Сокращённая ссылка: {short}")
    except Exception as e:
        bot.reply_to(message, f"Ошибка при сокращении ссылки: {e}")


# Путь к папке, где будет сохраняться база данных
db_folder = r"C:\Users\Ш\OneDrive\Desktop\tgbot\tgbots"  # Используем raw-строку для корректной обработки пути

# Убедимся, что папка существует
os.makedirs(db_folder, exist_ok=True)

# Путь к базе данных
db_path = os.path.join(db_folder, "activity.db")

# 🗃️ Подключение к базе данных
conn = sqlite3.connect(db_path, check_same_thread=False)
cursor = conn.cursor()

# 🧱 Создание таблицы, если её нет
cursor.execute('''
    CREATE TABLE IF NOT EXISTS message_stats (
        chat_id INTEGER,
        user_id INTEGER,
        message_count INTEGER DEFAULT 0,
        PRIMARY KEY (chat_id, user_id)
    )
''')
conn.commit()

# ⬆️ Увеличение счётчика сообщений
def increment_message_count(chat_id, user_id):
    with conn:
        conn.execute('''
            INSERT INTO message_stats (chat_id, user_id, message_count)
            VALUES (?, ?, 1)
            ON CONFLICT(chat_id, user_id) DO UPDATE SET
                message_count = message_count + 1
        ''', (chat_id, user_id))


# 📊 Команда "/сообщения" — общее количество сообщений в чате
@bot.message_handler(commands=['сообщения'])
def send_message_count(message: Message):
    with conn:
        cur = conn.cursor()
        cur.execute('SELECT SUM(message_count) FROM message_stats WHERE chat_id = ?', (message.chat.id,))
        total = cur.fetchone()[0] or 0
    bot.send_message(message.chat.id, f"📊 В чате отправлено сообщений: {total}")


# 🔥 Команда "/актив" — топ 10 активных пользователей
@bot.message_handler(commands=['актив'])
def send_active_users(message: Message):
    with conn:
        cur = conn.cursor()
        cur.execute('''
            SELECT user_id, message_count FROM message_stats
            WHERE chat_id = ?
            ORDER BY message_count DESC
            LIMIT 10
        ''', (message.chat.id,))
        top_users = cur.fetchall()

    if not top_users:
        bot.send_message(message.chat.id, "Активности пользователей пока не зафиксировано.")
        return

    active_users_message = "🔥 <b>Самые активные пользователи чата:</b>\n"

    for i, (user_id, msg_count) in enumerate(top_users, 1):
        try:
            user = bot.get_chat_member(message.chat.id, user_id).user
            username = user.username or user.first_name
            active_users_message += f"{i}. {username} — {msg_count} сообщений\n"
        except Exception:
            continue

    bot.send_message(message.chat.id, active_users_message, parse_mode='HTML')


# 📥 Отслеживание всех входящих сообщений
@bot.message_handler(func=lambda message: True)
def track_messages(message: Message):
    chat_id = message.chat.id
    user_id = message.from_user.id

    # 📈 Обновляем счётчик
    increment_message_count(chat_id, user_id)

    # 🔁 Проверяем пороги
    with conn:
        cur = conn.cursor()
        cur.execute('SELECT SUM(message_count) FROM message_stats WHERE chat_id = ?', (chat_id,))
        total_count = cur.fetchone()[0] or 0

    # Мотивации
    reactions = ["👍", "😄", "🔥", "🎉", "👏", "💡"]
    motivational_messages = [
        "💪 Никогда не сдавайся, каждый шаг приближает тебя к цели!",
        "🚀 Делай всё, что в твоих силах, и результат не заставит себя ждать!",
        "🌟 Самое сложное — начать. Начни прямо сейчас!",
        "🔥 Успех приходит к тем, кто усердно работает и верит в себя!",
        "🎯 Цель без плана — это просто мечта. Планируй и действуй!"
    ]

    if total_count % 1000 == 0:
        username = message.from_user.username or message.from_user.first_name
        bot.send_message(chat_id, f"{username} на пенисе! 😆🍆")
    elif total_count % 150 == 0:
        bot.send_message(chat_id, random.choice(reactions))
    elif total_count % 75 == 0:
        bot.send_message(chat_id, random.choice(motivational_messages))


# (Опционально) Для отладки — покажет любое сообщение
@bot.message_handler(func=lambda msg: True)
def debug_all_messages(message):
    print(f"[DEBUG] Получено сообщение: {message.text!r}")

# Запуск бота
bot.polling(none_stop=True)